# 扩展任务 02：角色首页与提交/审批工作台（占位）

> 状态：**历史占位；任务 02 已完成**。正式工作台、提交/审批与相关回归已落地，当前事实见根目录 `HANDOFF.md`；下文保留为最初占位，不作为当前状态。

## 情况说明

任务 02 是"两角色首页、评测列表、提交/审批闭环"。据[执行计划](../../../../../.scratch/ui-catalog-providers/plan.md)与[实现地图](../../../../../.scratch/ui-catalog-providers/implementation-map.md)，该任务**已经实现并验收**（A「侧栏工作台」、32 条浏览器回归、类型检查、生产构建、API 对账与双轴评审），但本 Module 的文档结构建立时（2026-09-20）尚无对应的正式行动文档落到本目录。

## 依赖（据分工，供后续补正式行动时引用）

- 扩展任务 01 的原型与选定布局；
- **A（身份与成员）**的 Identity/Membership Interface（会话、角色、邀请与成员管理）；
- **D（Job 控制）**的 Job Interface（提交、批准/拒绝、取消、恢复/重试、报告与证据）。

## 本文件不做的事

- 不复制逐控件契约表（权威在[实现地图](../../../../../.scratch/ui-catalog-providers/implementation-map.md)第 2.2 节）。
- 不复制 HTTP 契约（权威在 [`HTTP_API.md`](../../../../interfaces/HTTP_API.md)）。
- 不把任务 02 的完成当作任务 03 的开工授权。

## 待确认

任务 02 是否需要在本目录补一份正式行动文档（含实际文件树与验证证据），还是沿用既有任务 02 行动记录，需 B 确认后再决定。在确认之前本文件保持占位，不新增内容。
