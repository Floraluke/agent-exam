# 任务 05 实施方案编写与本机 Docker 前提变更记录

## 状态与情况

- 状态：已完成（方案就绪；实施与执行授权均未取得）。
- 来源请求：用户询问"可以写实施方案开工了吗"，并告知其本机已安装 Docker。
- 当前事实（已核对）：
  - **任务 05 仍未获实施开工授权**。负责人 2026-09-21 回执原文："负责人决定已经允许维护人把第 1 步的数值部分写入设计冻结记录，**但本次没有授予任务 05 的实施开工许可**。第 2–4 步仍须等待任务状态和开工授权；第 5 步还须等待单独的机器窗口与执行授权。"其本轮授权边界为"只填写文档、同步 `fengyy-fixweb`、提交并普通推送；**不运行拓扑探针，不创建或删除 Docker 资源**"。任务单状态仍为 `needs-info`。
  - **本机已安装 Docker**：`docker` CLI 29.6.2 位于 `C:\Program Files\Docker\Docker\resources\bin`，Docker Desktop 已安装；但**守护进程当前未运行**（`docker ps` 报 named pipe 不存在，进程中无 Docker Desktop），WSL 存在 Ubuntu-22.04。
  - **`framework/harbor` 仍不在本机**（`.gitignore` 排除，只在负责人机器上）。因此"固定 Harbor 是否允许替换网络附加方式"这一关键问题仍只能由负责人机器回答。
  - 阶段 0 当时**明确决定不安装 Docker**，理由是"Docker 依赖虚拟网络，与本机现有的网络驱动问题叠加会放大风险"（同机 Tailscale 的 wintun 虚拟网卡装不上）。本机安装 Docker 因此是对该决策的**变更**，需要记录，且其风险点尚未验证。
- 已确认决定：本次产出**实施方案文档**（属准备材料，与此前被接受的测试设计、设计冻结同类，不属"实施"），并记录 Docker 前提变更；**不写产品代码**（未获开工授权），**不启动 Docker Desktop、不创建任何容器或网络**（未获执行授权，且属机器状态变更）。
- 明确排除：不实现 `provider_access/` 或任何源码目录，不运行拓扑探针，不修改产品代码，不新建 Module/Interface/表，不代负责人扩大授权。

## 实施措施

1. 编写实施方案：按实现地图第 3 节的候选树列出实际文件树与职责、分片顺序（S1–S11）、每片的验证方式与运行位置、以及与拓扑结论的依赖关系。
2. 在方案中把拓扑实证拆成两半——纯 Docker/Compose 层（本机可做）与固定 Harbor 集成层（仍须负责人机器）——并说明拆分依据与各自要回答的问题。
3. 在方案中单列"待授权清单"：实施开工授权、拓扑执行窗口；并单列"本机 Docker 前提变更"的影响与待验证风险。
4. 更新 `docs/LLY/02-environment/LOCAL_SETUP.md` 中已失效的"Docker 未安装/未运行"表述，改为记录实际状态与变更来源，同时保留原决策的理由与未验证风险。
5. 同步进度日志与 README，运行文档层验证后提交推送。

完成标准：实施方案给出可执行的文件树、分片、验证方式与机器归属；明确区分"本机可做"与"须负责人机器"；不把未获授权的事项写成已获授权；Docker 前提变更被如实记录且未把风险写成已解决。

## 受影响文件树

```text
docs/LLY/01-plan/
└─ STAGE1_IMPLEMENTATION_PLAN.md      # 新增：05 实施方案（文件树、S1–S11 分片、验证方式、机器归属、待授权清单）
docs/LLY/02-environment/
└─ LOCAL_SETUP.md                     # 修改：Docker 前提由"未安装"改为实际状态与变更记录
docs/LLY/
├─ README.md                          # 修改：01-plan 补充实施方案入口
└─ 03-progress/PROGRESS_LOG.md        # 修改：追加本次记录与当前停点
docs/actions/
└─ 2026-09-21-task05-implementation-plan.md  # 新增：本行动记录
```

设计关系：本行动只新增方案文档与状态记录，不创建源码目录、不新增 Module/Interface/表，不改变模块边界与依赖方向。方案中的路径引用实现地图第 3 节的候选树，本次不在磁盘上创建。

## 自验证方式

1. 边界检查：确认方案明确写出"未获实施开工授权"，且未把 S2–S9 描述为可立即执行；确认"待授权清单"存在。
2. 机器归属检查：逐片确认"本机/负责人机器"判断与本机实际能力一致（本机有 Docker CLI 但守护进程未运行、无 `framework/harbor`）。
3. 文件树一致性：确认方案的文件树与实现地图第 3 节候选树一致，未凭空新增产品目录。
4. 前提变更检查：确认 `LOCAL_SETUP.md` 不再声称 Docker 未安装，且保留原决策理由与新风险。
5. 敏感值扫描：确认不含真实 Key、账号、tailnet 地址、私有绝对路径。
6. 链接与格式：文档内相对链接可解析、代码围栏成对、无行尾空格；`git status` 只出现本次预期文件。
7. 不运行产品测试：本行动只产出方案与文档，运行测试不适用；不得以方案存在冒充任何实现、前置或断言已通过。

## 自验证情况

- **边界检查通过：** 方案首行即写明"方案就绪，未获实施开工授权"，并引用负责人回执原文；第 6 节单列"待授权清单"三项。S2–S9 均标注"E 本机"可做，但方案第 3 节开头明确"S2–S9 不依赖拓扑结论，可与拓扑实证并行"——**这是依赖关系描述，不是开工许可**，第 6 节已把实现授权列为未取得。
- **机器归属检查：** 逐片核对与本机实际能力一致。实测本机 `docker` CLI 29.6.2 可执行但守护进程未运行（`docker ps` 报 named pipe 不存在、进程列表无 Docker Desktop）；`framework/harbor` 不存在（`ls -d` 失败）。因此 S10/S11 与 T2 归负责人机器、S2–S9 归本机、T1 标为"本机（新可能）"并注明其前置（本机能否创建自定义网络未验证）。
- **文件树一致性：** 方案第 2 节逐行取自实现地图第 3 节"05–07 API 绑定与隔离代理"候选树，未新增产品目录；并记录两处边界事实——`provider_access/` 恰 8 文件已达每层上限、`tests/providers/` 同为上限边界。
- **前提变更检查：** `LOCAL_SETUP.md` 三处（目标行、决策表行、已知限制行）均已不再声称 Docker 未安装；决策表行改为记录变更、来源与"自定义网络能力尚未验证"，并保留原决策理由。`STAGE1_PROXY_TEST_DESIGN.md` 的环境前提段与风险表行同步更新，且明确标注"运行位置列暂保留原编排，T1/T2 拆分获确认后同步"。历史行动记录未改写（保留当时真实状态）。
- **未越权检查：** 任务单已批准的第 2 项验收（"组长机器"）**未修改**；改为在 Comments 记录变更事实与 T1/T2 提案，并写明"未获负责人批准前不执行拆分、不擅自修改已批准验收项"。过程中曾误写入占位链接 `aaa`，已修正为 `../../../docs/LLY/01-plan/STAGE1_PROXY_TEST_DESIGN.md` 并复验可解析。
- **敏感值扫描：** 对 `61A17959`、`sss.tail03c757`、`sk-`、`D:\`、`C:\`、`E:/` 六个模式扫描新增与修改文档，命中均为 0。
- **链接与格式：** `docs/LLY/` 全部相对链接可解析；任务单 Comments 新增段落的链接可解析；代码围栏成对；未检出文件行尾空格。
- **工作区：** 本切片含 2 个新增文件（实施方案、本行动文档）、4 个修改文件（`LOCAL_SETUP.md`、`STAGE1_PROXY_TEST_DESIGN.md`、任务单、`PROGRESS_LOG.md`、`README.md`）。
- **未运行的检查：** 未启动 Docker Desktop，未创建任何容器/网络/卷，未运行任何断言，未写一行产品代码，未读取真实凭据，未运行产品测试。**不得以方案存在冒充任何实现、前置或断言已通过**——前置仍为 3/6、7 条断言仍未执行。
- **遗留与风险：**
  1. 三项授权均未取得；方案不能替代授权，实施与执行都不得据本方案启动。
  2. 本机 Docker 能否创建自定义网络**未验证**；若因虚拟网络问题不能，T1 提案作废，需退回"两半都在负责人机器"。
  3. T1/T2 拆分提案将改变任务单第 2 项验收的机器归属，须由负责人批准后方可同步该验收项；批准前任务单保持原样。
  4. `STAGE1_PROXY_TEST_DESIGN.md` 第 2 节的"运行位置"列与第 3 节的用例归属仍按原编排（集成层全部标"仅组长机器"）；T1 若获批需要一次一致性 pass。
  5. 本机安装 Docker 是对阶段 0 决策的变更；原决策理由（虚拟网络风险）在新用途下是否成立仍未验证。
  6. 本次未提交、未推送 Git（由用户确认）。
