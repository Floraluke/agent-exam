# 起草任务 05（假提供方安全执行链）任务单

## 状态与情况

- 状态：已完成。
- 来源请求：用户询问 05 的归属、是否轮到自己以及对"发布"含义的疑问，随后要求起草 05 的任务单。
- 当前事实（已实际核对）：
  - 05 的任务 DRI 为 E，见 `TEAM_WORK_ALLOCATION.md` 第 1 节与第 5 节任务表；参与模块含 C（受控配置）、D（额度/状态/报告）、A（私有文件/容器拓扑）、B（必要错误呈现）。
  - `.scratch/ui-catalog-providers/issues/` 当前只有 `01-`、`02-`、`04-` 三份任务单，**05 从未发布**。
  - 05 的技术前置为"用户安排 API 阶段；先确认候选内部树与安全实现合同"，**无技术硬前置**（计划第 7 节）。
  - 05 的第一步是"最小拓扑实证"，需要容器与网络隔离；本机按阶段 0 决定不安装 Docker，`framework/`、`runtime/` 只在组长机器，因此该步与集成层在本机无法执行。
  - 已有事实基线：固定 CLI `0.153.0` 的禁外网假令牌配置探针 5/5 通过（研究第 6.1 节），但只证明请求路由与配置拒绝，**不证明**真实供应商、代理隔离或新题可用；现有网络侧车只有过滤能力，无密钥托管或认证头注入能力（认证接口第 4.1 节）。
  - 实现地图第 5 节明示三项**尚未验证**：Token 上界、请求字段白名单、账本具体格式。
- 已确认决定：本次只**起草**任务单文件，等待项目负责人发布与开工授权。起草属于"准备"，不碰产品代码，与"提前修改后续任务代码"是两回事；对照依据是成员 C 为任务 04 走的同一条路（04 注释原文："2026-09-20 由成员 C 起草，等待项目负责人发布与开工授权"）。
- 明确排除：不实施 05、不创建 `adapters/execution/provider_access/` 或任何源码目录、不修改产品代码、不安装 Docker、不读取真实 Key、不发起真实调用、不充值、不提交或推送 Git。

## 实施措施

1. 按 `docs/agents/issue-tracker.md` 的本地任务约定确定文件路径与命名：`.scratch/ui-catalog-providers/issues/05-<slug>.md`，`Status:` 置于文件首行。
2. 比对同目录 04 任务单的实际结构，沿用同一骨架：`What to build`、`Blocked by`、`Spec stories`、带复选框的验收项、`## Comments`。
3. 验收项来源限定为两处权威：`plan.md` 第 7 节的验收与停止条件、`verification.md` 第 4 节"代理与限额"与第 2 节需求覆盖表的 Q10/Q13/Q14；不新造权威来源之外的验收要求。
4. 在 `Blocked by` 与 `Comments` 中如实列出硬依赖与尚未验证的技术未知，**不把候选写成已确认**，也不把 05 描述为可立即开工。
5. 在 `Comments` 中记录起草依据、已核对的既有事实、本机限制，以及 E 侧已有的准备产物（`docs/LLY/01-plan/STAGE1_PROXY_TEST_DESIGN.md`）。
6. 同步 `PROGRESS_LOG.md` 与本目录 README 入口。

完成标准：任务单落在 `issues/05-*.md`；`Status:` 取值属于标签映射允许的五个值之一；每条验收项可追溯到 `plan.md` 第 7 节或 `verification.md` 的对应条目；硬依赖与未验证项均显式标注；无任何"已实现/已验证/可立即开工"的表述。

## 受影响文件树

```text
.scratch/ui-catalog-providers/issues/
└─ 05-fake-provider-secure-execution-chain.md   # 新增：05 任务单草案（Status: needs-info，待负责人发布）
docs/LLY/
└─ 03-progress/PROGRESS_LOG.md                  # 修改：追加 2026-09-21 起草记录与当前停点
docs/actions/
└─ 2026-09-21-draft-task-05-issue.md            # 新增：本行动记录
```

设计关系：本行动只新增一份任务单草案与过程记录，不创建源码目录、不新增 Module/Interface/表，不改变模块边界与依赖方向。任务单中提到的 `provider_access/` 等路径引用实现地图第 3 节的**候选**树，本次不在磁盘上创建它们。

## 自验证方式

1. 结构对照：与 `04-five-new-tasks-and-continuous-scale.md` 逐节比对，确认骨架一致（首行 `Status:`、四段元信息、复选框验收项、`Comments`）。
2. 标签合规：`Status:` 取值必须在 `docs/agents/triage-labels.md` 的五个值之内。
3. 验收项可追溯：逐条标注其来源（`plan.md` 第 7 节验收/停止，或 `verification.md` 第 4 节分组、第 2 节 Q 编号）；不出现无来源的新要求。
4. 事实一致性：文中"未验证/候选/待冻结"的表述与 `implementation-map.md` 第 5 节、`CODEX_AUTHENTICATION.md` 第 4.1 节一致；不把 5/5 探针写成代理隔离已通过。
5. 边界检查：文中不出现真实 Key、私有绝对路径、真实账号或 tailnet 地址等敏感值。
6. 链接与格式：文档内相对链接可解析、代码围栏成对、无行尾空格；`git status` 只出现本次预期文件。
7. 不运行产品测试：本行动不产生可运行代码，运行测试不适用；不得以任务单存在冒充任何验收项已通过。

## 自验证情况

- **结构对照通过：** 与 `04-five-new-tasks-and-continuous-scale.md` 逐节比对，骨架一致——首行 `Status:`、`What to build`、`Blocked by`、`Spec stories`、复选框验收项、`## Comments`；另按计划第 7 节补一段 `停止（阻断真实 Key）`，该文字直接取自计划第 7 节的停止条件，非新增要求。
- **标签合规：** `Status: needs-info`，属于 `docs/agents/triage-labels.md` 五个允许值之一；全勾选前的草案不使用 `ready-for-agent`。
- **验收项追溯（覆盖核对发现 1 处遗漏，已修复）：** 9 项验收逐条对照计划第 7 节验收句与 `verification.md` 第 4 节五组负例。第一轮发现权威负例第一条的"未批准、错误 profile、缺密钥、宽权限/链接文件"未落入第 4 项，已补；随后关键短语扫描又发现权威措辞"合成 Run"未出现，已对齐第 5 项措辞。复扫结果：未批准、零命中、跨 Run、未知 usage、旧 ChatGPT、新增数据库表、第二执行接口 全部命中。
- **事实一致性：** 文中"未验证/候选/待冻结"的表述与 `implementation-map.md` 第 5 节、`CODEX_AUTHENTICATION.md` 第 4.1 节一致；5/5 配置探针明确标注"只证明配置加载与请求路由，不证明代理隔离"，未升级为通过。
- **敏感值扫描：** 对 `61A17959`、`sss.tail03c757`、`D:\`、`C:\Users`、`sk-` 等 7 个关键词扫描，命中均为 0；文中不含真实 Key、私有绝对路径、账号或 tailnet 地址。
- **链接与格式：** 该任务单 7 条相对链接全部可解析（含 `../plan.md`、`../implementation-map.md`、`../../../docs/...` 三类）；`docs/LLY/` 全部链接可解析；代码围栏成对；未检出文件行尾空格。
- **工作区：** `git status --short` 只出现本次预期文件——2 份修改的 LLY 文档、2 个新增文件（任务单草案与行动文档）。
- **未运行的检查：** 未创建 `tests/providers/` 或任何源码目录，未编写产品代码，未安装 Docker，未读取真实 Key，未发起真实调用，未运行产品测试。本行动只产出任务单草案，运行测试不适用；**不得以任务单存在冒充任何验收项已通过**——9 项验收全部为未勾选的 `[ ]`。
- **遗留与风险：**
  1. 任务单为草案，依赖负责人发布与开工授权；发布前不得据其开工。
  2. 第 2 项验收与集成层只能到组长机器执行，排期依赖组长机器窗口；本机无 Docker。
  3. 三项安全未知（Token 上界、请求字段白名单、账本格式）须在开工时先冻结，届时可能需要修订本任务单的验收措辞。
  4. 任务 04 中 E 的 12 h 配合范围已由 C 在组长机器完成，工时归属与任务 08 输入需与负责人确认；本次未据此修改分工表。
  5. 本次未提交、未推送 Git。
