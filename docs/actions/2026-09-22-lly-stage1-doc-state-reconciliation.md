# 行动：阶段 1 文档状态同步（修正过期状态与一处自相矛盾）

> 状态：**已完成**（2026-09-22）。纯文档改动，不改任何代码、不改权威契约（`HTTP_API.md` 等不在本次范围）。

## 1. 情况说明

**来源**：用户 2026-09-22 会话内确认"按你的改吧"，同意修正 `docs/LLY/` 中与实际进度不符的状态表述，并同意把 S9 的口径统一为"等 T2"。

**问题性质**（不是"旧文档 vs 新文档"，而是**仍在生效的计划文档**未随进度更新）：

| 类型 | 具体位置 | 与真实状态的差异 |
|---|---|---|
| 同一份文档内部自相矛盾 | `01-plan/STAGE1_IMPLEMENTATION_PLAN.md` 首行状态行与分片表 | 表内 S3–S7 已标"已完成"，同表 S2、`service.py`、S8 仍写"未做"，而三者实际已完成 |
| 跨文档矛盾 | 同文第 3 节"**S2–S9 不依赖拓扑结论**" vs `01-plan/STAGE1_PROXY_SERVICE_PLAN.md` 第 3 节"**S9 同样依赖 T2**" | 两处口径冲突，读者无法判断 S9 能否开工 |
| 状态行过期 | 同文第 2 节文件树（写"S 本次不创建"）、`tests/providers/runtime/` 文件数、待授权清单第 3 项（写"窗口为空"） | 文件树与子目录已实际建成；T2 窗口自 2026-09-21 起已可用 |
| 状态行过期 | `01-plan/STAGE1_PROXY_TEST_DESIGN.md` 首行"准备性设计，未开工" | 该设计的用例归属已在 `tests/providers/` 落地 |
| 状态行过期 | `01-plan/PLAN.md` 首行"产品开发未开工"、阶段 0 标题"（当前阶段）" | 阶段 1 本机部分已完成 |
| 状态行过期 | `README.md` 第 39 行"05 未开工" | 同上 |

**已确认的决定**：S9 的口径统一为"**依赖 T2**"（依据较新的 `STAGE1_PROXY_SERVICE_PLAN.md` 第 3 节与任务单 Comments 的既有记录；用户 2026-09-22 同意）。理由：S9 决定的是"worker 按 Run 选绑定"的接法，而绑定形态（代理入口、令牌来源、网络附加方式）由 T2 的拓扑结论决定。

**明确不做**：不改代码、不改 `HTTP_API.md` 与 `DATA_MODEL.md`、不改 `.scratch/` 下的权威计划与任务单正文（只在任务单 Comments 追加事实）、不改已归档的历史行动记录（历史记录保留当时真实状态）、不改 `TASK05_OWNER_DELIVERY_FILLED.md`（其第 0–6 节已标注为"首次核对当时状态"）。

## 2. 实施措施

1. `STAGE1_IMPLEMENTATION_PLAN.md`：首行状态行、维护人日期、第 2 节文件树（改为**实际已建**的树并把未建项标出）、第 2 节末的目录上限说明与文件数、第 3 节口径句与分片表（S1–S11 逐行状态，S9 的"等拓扑"列改为"是"）、第 5 节 T1 前置句改为已完成、第 6 节待授权清单第 1、3 项。
2. `STAGE1_PROXY_TEST_DESIGN.md`：首行状态行改为"本地实施已完成、集成层等 T2"，并指向落地的用例目录；正文归属表不再改动（其环境前提段已于 2026-09-21 修正）。
3. `PLAN.md`：首行状态行改为"阶段 1 本机部分已完成、S9–S11 等 T2"，阶段 0 标题标"已完成"，阶段 1 标题标"本机部分已完成"。
4. `README.md`：第 39 行测试设计说明改为与落地状态一致。
5. 逐条核对：改动后的每一句都能指回代码或进度日志；`未做`/`未开工` 等词扫描后只应剩正确用法。

**完成标准**：四份文档不再出现与真实进度冲突的状态表述；S9 依赖口径在 `docs/LLY/` 内唯一；相对链接全部可解析。

## 3. 受影响文件树

| 路径 | 改动与职责 |
|---|---|
| `docs/LLY/01-plan/STAGE1_IMPLEMENTATION_PLAN.md` | **改**。阶段 1 的实施方案与分片表，本次修正状态、实际文件树与待授权清单。 |
| `docs/LLY/01-plan/STAGE1_PROXY_TEST_DESIGN.md` | **改**（仅首行状态）。测试归属与运行位置的准备性设计。 |
| `docs/LLY/01-plan/PLAN.md` | **改**（仅状态与两个阶段标题）。E 模块本地计划书。 |
| `docs/LLY/README.md` | **改**（仅第 39 行说明）。`docs/LLY/` 的目录说明与当前状态索引。 |
| `docs/actions/2026-09-22-lly-stage1-doc-state-reconciliation.md` | 本文件（新增）。 |

未改动：任何 `.py`、`HTTP_API.md`、`DATA_MODEL.md`、`.scratch/` 权威计划正文、任务单正文（仅 Comments 追加）、历史行动记录。

## 4. 自验证方式

| 检查 | 手段 | 期望 |
|---|---|---|
| 状态与代码一致 | 逐句对照 `git ls-files` 实际文件、`wc -l` 文件数、进度日志实测数字 | 无冲突 |
| 关键词扫描 | 在 `docs/LLY/` 内检索 `未做`、`未开工`、`窗口为空`、`未创建` | 仅剩正确用法（历史归档与"未做项"清单） |
| 链接可解析 | 脚本解析本次改动文件的相对链接并校验存在性 | 缺失 0 |
| 口径唯一 | 检索 `S9` 相关表述，确认全部为"等 T2" | 无相反表述 |

本行动不涉及代码，故不运行 `ruff`/`mypy`/`pytest`；这一限制在自验证情况中如实记录。

## 5. 自验证情况

**已执行，且实际输出已检查**：

| 检查 | 手段 | 实际结果 |
|---|---|---|
| 状态与代码一致 | `git ls-files` + 文件计数对照：`provider_access/` 顶层 7 文件、`server/` 8 文件、`tests/providers/{policy,lifecycle}` 各 8/7 文件、`runtime/` 5 文件；用例总数 `pytest --collect-only` 166 | 与改后的文件树逐行一致 |
| 实测数字 | 与[进度日志](../LLY/03-progress/PROGRESS_LOG.md)当日记录核对（`tests/providers` 165 passed / 1 skipped、默认回归 587/105/2，修复后 591） | 一致 |
| 关键词扫描 | `grep -rn "未开工\|窗口为空\|产品开发未开工" docs/LLY/` | 仅剩**历史引用**（进度日志对归档前旧版的描述）与**归档原件**（`TASK05_OWNER_*` 三份为"请求→回复→回执"原件，保留当时状态）；无仍生效的状态表述过期 |
| `未做` 扫描 | 同上 | 仅剩 S9–S11 的**正确**用法 |
| S9 口径唯一 | `grep -rn "S9" docs/LLY/` 逐条判断 | 8 处全部为"等 T2 / 依赖 T2"，无相反表述 |
| 相对链接 | 脚本解析 8 个改动文件的相对链接并校验存在性 | **缺失 0** |

**计划偏差**：比计划多做两处，均为同类过期表述，已如实写入"受影响文件树"与进度日志：`STAGE1_PROXY_TEST_DESIGN.md` 第 5 节"开工前必须冻结"四项的现状、第 6 节风险表三行（05 已发布、04 已完成、T2 窗口已可用）。

**未执行与限制**：

- **未运行 `ruff` / `mypy` / `pytest`**：本次改动全部是 `.md`，不含代码；当日两套测试的实际结果见另一行动的记录。
- 未改动 `HTTP_API.md`、`DATA_MODEL.md` 与 `.scratch/` 权威计划正文；未改动"请求→回复→回执"三份归档原件；未改动历史行动记录（历史保留当时真实状态）。
- 未解决的遗留：任务单 Comments 建议的**负责人书面确认**（实施开工 + T1/T2 拆分）仍未补；`STAGE1_PROXY_DESIGN_FREEZE.md` 的机制部分仍标候选（属设计状态，不是过期表述）。
