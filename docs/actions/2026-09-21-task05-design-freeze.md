# 任务 05 代理设计冻结（第 1 步工作底稿）

## 状态与情况

- 状态：已完成（底稿产出；设计本身**尚未冻结**，待负责人对第 4 节 9 项拍板）。
- 来源请求：用户要求提交推送后"先把我这边的完成了，再写一份详细完整的负责人那边拿到能直接照做的文件，我再合并上去"。
- 当前事实（本轮实际核对）：
  - 05 的第 1 步是"冻结代理拓扑、Key 文件格式/权限验证、可允许请求字段与计量策略"（计划第 7 节步骤 1）。该步是**设计活动**，不依赖容器，可在 E 本机完成；它也是后续实现的唯一依据，跳过它写代码会把未验证假设编码成断言。
  - 已从研究第 1 节取得一条此前未纳入设计的硬约束：Codex 官方配置含 `request_max_retries`、`stream_max_retries`，**默认值不是零**，必须显式关闭并由代理验证没有重发。
  - 已从现有代码确认：`adapters/execution/network.py` 的侧车是 `harbor-docker-egress-control-sidecar`（gost + nftables allowlist），且 `compose_profile()` **刻意不声明 `networks`**，由 Harbor 环境附加自己的侧车——主容器与侧车共享网络命名空间，因此不能直接承载"只通代理"的边界（与认证接口第 4.1 节第 5 条的警告一致）。
  - 研究第 4 节已给出首轮验收的额度初值（输入 30 万 / 输出 3.2 万 / 期限 900 秒 / 频率 3 次每分钟 / ¥100），但这些是**工程初值，不是已生效限制**，其中若干项属 owner 决定。
- 已确认决定：本次只产出**设计冻结底稿**，不实现代理代码、不创建源码目录、不安装 Docker、不读取真实或假的上游、不把候选写成已冻结。
- 明确排除：不修改产品代码，不新建 Module/Interface/表，不提交或推送 Git（提交由用户单独确认）；不代替 owner 决定额度与账户侧事项。

## 实施措施

1. 汇总可直接依据的既有事实（含研究第 6.1 节已通过的固定 CLI 配置探针结论与第 1 节的默认重试事实），避免后续实现重新验证同一件事。
2. 逐项给出待冻结项的候选设计：代理拓扑、私有文件格式与权限拒绝条件、请求字段白名单与拒绝规则、计量与账本机制、令牌绑定与生命周期、配置渲染。
3. 每项明确标注"候选/未决/待负责人拍板"，把机制设计与数值决定分开——机制由 E 定，额度与账户侧事项交负责人。
4. 单列"须在开工前由负责人拍板"的清单，作为交接给对方的最小决策集合。
5. 同步 `docs/LLY/` 的进度日志与 README 入口。

完成标准：六项待冻结内容各有候选设计且标注状态；所有可依据的事实给出权威来源；额度类数值全部归入"待负责人拍板"而非自行设定；拓扑一项如实写明"须由组长机器实证"并指出第一个要回答的问题；不出现任何真实凭据或私有绝对路径。

## 受影响文件树

```text
docs/LLY/01-plan/
└─ STAGE1_PROXY_DESIGN_FREEZE.md      # 新增：05 第 1 步的候选设计冻结底稿与待拍板清单
docs/LLY/
├─ README.md                          # 修改：01-plan 补充该文件入口
└─ 03-progress/PROGRESS_LOG.md        # 修改：追加本次记录与当前停点
docs/actions/
└─ 2026-09-21-task05-design-freeze.md # 新增：本行动记录
```

设计关系：本行动只产出设计底稿，不创建源码目录、不新增 Module/Interface/表，不改变模块边界与依赖方向。文中提到的 `adapters/execution/provider_access/`、`codex/provider_config.py` 引用实现地图第 3 节的**候选**树，本次不在磁盘上创建。

## 自验证方式

1. 事实溯源：逐条检查第 2 节"已有事实"均标注权威来源（研究第 1 节 / 第 6.1 节 / 认证接口第 4.1 节 / 现有 `network.py`）。
2. 数值归属：确认所有额度、预算、频率、期限数值均在"待负责人拍板"清单内，未在机制章节被写成已定值。
3. 状态标注：确认拓扑、计数方式、私有文件路径三项明确标为候选或未决，未被写成已确认。
4. 与权威一致：检查"未知字段一律拒绝""不退回未知用量""Run 剩余额度不因代理重启重置""默认重试须显式关闭"等表述与研究第 5 节、第 1 节一致，未放宽也未加严。
5. 敏感值扫描：确认不含真实 Key、真实账号、tailnet 地址、私有绝对路径。
6. 链接与格式：文档内相对链接可解析、代码围栏成对、无行尾空格；`git status` 只出现本次预期文件。
7. 不运行产品测试：本行动不产生可运行代码，运行测试不适用；不得以设计文档存在冒充任何设计项已冻结或已验证。

## 自验证情况

- **事实溯源通过：** 第 2 节四条"可直接依据的事实"逐条标注来源——固定 CLI 配置探针结论（研究第 6.1 节）、命令行可覆盖模型（研究第 6.1 节）、默认重试非零（研究第 1 节）、侧车 `compose_profile()` 不声明 `networks`（读 `adapters/execution/network.py` 实际源码核对）。无未标注来源的事实陈述。
- **数值归属检查通过：** 全文所有额度、预算、频率、期限数值（30 万 / 3.2 万 / 900 秒 / 3 次每分钟 / ¥100 / ¥80）均只出现在第 4 节"待负责人拍板"表与对研究初值的引用中，第 3 节机制部分未把它们写成已定值。
- **状态标注检查通过：** 拓扑标为"候选；结论须由组长机器实证"并指出第一个待答问题；输入 Token 计数方式标为"未决"；私有文件路径与 owner 主体列入待拍板；文档首行标"候选设计，尚未冻结"。
- **与权威一致性检查：** "未知字段一律拒绝""usage 缺失不退回未知部分、不以客户端没收到推断没计费""Run 剩余额度不得因代理重启重置""失去已花费证据时失败关闭""禁用内置付费能力""默认重试须显式关闭"等表述逐条对应研究第 5 节第 1–4 条与第 1 节，未放宽也未加严。
- **敏感值扫描：** 对 `61A17959`、`sss.tail03c757`、`sk-`、`D:\`、`C:\`、`E:/` 六个模式扫描，命中均为 0；全文未检出真实 Key、账号、tailnet 地址或私有绝对路径。
- **链接与格式：** 设计底稿 5 条相对链接（`../../interfaces/...`、`../../research/...`、`../../../.scratch/...`）全部可解析；`docs/LLY/` 全部链接可解析；未检出文件行尾空格；底稿 97 行。本轮两份文档均未使用代码围栏。
- **工作区：** 本次改动含 4 个文件——设计冻结底稿（新增）、本行动文档（新增）、`docs/LLY/README.md` 与 `docs/LLY/03-progress/PROGRESS_LOG.md`（修改）。
- **未运行的检查：** 未创建 `adapters/execution/provider_access/` 或任何源码目录，未编写产品代码，未安装 Docker，未读取真实 Key，未发起任何上游请求，未运行产品测试。本行动只产出设计底稿，运行测试不适用；**不得以设计底稿存在冒充任何设计项已冻结、任何拓扑或限额已验证**。
- **遗留与风险：**
  1. 设计**尚未冻结**：第 4 节 9 项未拍板前，第 3 节的候选设计不能作为实现依据；其中输入 Token 计数方式未决会直接影响账本能否承诺硬上限。
  2. 拓扑候选的可行性未知，且取决于固定 Harbor 上游是否允许替换网络附加方式；这只能由组长机器实证回答，本机无 Docker 无法预判。
  3. 研究第 4 节的额度初值来自 2026-09-17 的价格快照，正式使用前须重新核验官方价格与账户资格。
  4. 设计底稿与测试设计、组长机器预案三份文档存在内容交叠（均涉及私有文件与拓扑），本次按"权威正文只此一份、三份各自承担机制/测试/执行视角"处理；若后续出现不一致，以权威文档为准并同步三份。
  5. 本次未提交、未推送 Git。
