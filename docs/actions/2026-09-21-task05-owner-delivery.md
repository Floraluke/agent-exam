# 任务 05 负责人侧交付要求文档

## 状态与情况

- 状态：已完成。
- 来源请求：用户要求写一份**可直接发给负责人照做**的交付要求文档，覆盖三件事——拍板 9 项数值（主要是第 7 项输入 Token 计数方式）、按预案做前置核对、用最小替身先跑拓扑实证。
- 当前事实（已核对）：
  - 05 的机制设计已有底稿（`docs/LLY/01-plan/STAGE1_PROXY_DESIGN_FREEZE.md`），其中 **9 项数值与账户侧事项**等待负责人拍板；第 7 项（输入 Token 计数方式）是唯一会阻塞账本算法实现的选择。
  - 组长机器侧的执行预案已存在（`docs/actions/2026-09-21-task05-owner-machine-runbook.md` 第 05 节），但它是 E 侧过程文档，形态不适合直接转发给对方。
  - 项目已有"面向接收方自足、可单独发出"的先例（如 `TEAM_POSTGRESQL_CONNECTION.md` 作为发给组员的连接卡片），因此本次采用同类形态，同时不替代权威正文。
- 已确认决定：本次只新增一份**交付要求文档**，不实现代理代码、不创建源码目录、不安装 Docker、不读取真实 Key、不代为决定任何额度或账户侧事项。文档形态为"自足可转发 + 关键处指向权威位置"，而非复制权威正文。
- 明确排除：不修改产品代码，不新建 Module/Interface/表，不提交或推送 Git（由用户单独确认）。

## 实施措施

1. 把底稿第 4 节的 9 项拍板事项转成**可填写表格**，每项给出研究初值与建议，并对第 7 项单独展开两条路线的后果。
2. 把预案 B 节的前置核对整理为可逐项打勾的清单，标注"任一不满足即停"。
3. 把预案 C 节的最小拓扑实证整理为可顺序执行的步骤与断言清单，并明确**用一次性探针而非产品代码**，与 M0 探针的既有做法一致。
4. 明确代码归属：产品代码由 E 在本机编写并跑绿后推送；一次性探针（位于被 Git 忽略的证据目录、不进入产品树）允许在负责人机器上编写与运行，其输出摘要作为证据引用。
5. 写明"不要做"清单、必须回报的证据形态，以及本文与权威文档的对应关系（冲突时以权威为准）。

完成标准：负责人无需阅读其他文档即可照做；每一件待办都有明确动作、判断标准与产出；不出现"已实现/已验证"表述；不出现真实凭据、账号或私有绝对路径；额度与账户侧决定全部保留为待填，未被代为设定。

## 受影响文件树

```text
docs/LLY/01-plan/
└─ TASK05_OWNER_DELIVERY.md            # 新增：可直接转发给负责人的交付要求（三件事 + 填写表格 + 断言清单）
docs/LLY/
├─ README.md                           # 修改：01-plan 补充该文件入口及其"可转发"性质
└─ 03-progress/PROGRESS_LOG.md         # 修改：追加本次记录与当前停点
docs/actions/
└─ 2026-09-21-task05-owner-delivery.md # 新增：本行动记录
```

设计关系：本行动只新增一份交接文档，不创建源码目录、不新增 Module/Interface/表，不改变模块边界与依赖方向。文档中提到的 `provider_access/` 等路径引用实现地图第 3 节的候选树，本次不在磁盘上创建。

## 自验证方式

1. 自足性检查：确认文档包含完成三件事所需的全部动作、判断标准与产出，正文不要求读者先读其他文档。
2. 权威一致性：逐条核对断言与前置项对应认证接口第 4.1 节与计划第 7 节，未放宽也未加严；文件末尾给出权威对应位置。
3. 数值归属：确认 9 项全部保留为待填状态，未由本文件代为设定。
4. 边界检查：确认写入"不用真实 Key""不照搬现有侧车""不改共享 Docker/WSL/全局网络""不下载大体积镜像""不为通过而修改断言"。
5. 敏感值扫描：确认不含真实 Key、真实账号、tailnet 地址、私有绝对路径。
6. 链接与格式：文档内相对链接可解析、代码围栏成对、无行尾空格；`git status` 只出现本次预期文件。
7. 不运行产品测试：本行动不产生可运行代码，运行测试不适用；不得以交付文档存在冒充任何拓扑或限额已验证。

## 自验证情况

- **自足性检查通过：** 文档包含完成三件事所需的全部动作、判断标准与产出——第 1 节 9 项拍板表（含第 7 项两条路线的后果）、第 2 节 6 项前置核对清单、第 3 节 7 条断言与执行步骤、第 4 节禁令、第 5 节回报格式。正文不要求读者先读其他文档；权威位置集中在第 7 节文末。
- **权威一致性检查：** 第 3.1 节的"直连拒绝、宿主隔离、正常模型请求正对照"三组对照对应认证接口第 4.1 节第 5 条；断言 1–7 对应计划第 7 节步骤 1 的"做题容器仅能到专属代理；真实上游网络仅在代理侧；PID/文件系统隔离、无主机发布端口/容器套接字"；第 7 项计数要求对应研究第 5 节第 1 条。未放宽也未加严。
- **数值归属检查通过：** 9 项拍板表格中留有 9 个空单元格（实测 `grep -c "| |$"` = 9），全部为待填；第 1 节列出的初值均标注为"工程初值（建议起点）"，未写成已定值。第 7 项明确写出"如果不选，E 只能按 A 做并写明不是精确账单"，未代为选择。
- **边界检查通过：** 第 3.2 节写明现有侧车"与主容器共享网络命名空间、不能承载该边界"，并指出能否替换是**本片第一个要回答的问题**；第 4 节含"不用真实 Key / 不读取登录文件 / 不充值""不放宽到公网""不把真实 Key 放进做题容器""不照搬现有网络侧车""不改共享 Docker/WSL、全局代理、防火墙""不下载大体积镜像""不为让断言通过而修改断言或跳过条目"。
- **代码归属已澄清：** 第 3.3 节明确一次性探针允许在负责人机器上编写与运行（沿用 M0 探针做法、位于 Git 忽略的证据目录、不进产品树），第 6 节明确产品代码仍由 E 本机写好跑绿后推送。此前的表述歧义在此消除。
- **敏感值扫描：** 对 `61A17959`、`sss.tail03c757`、`sk-`、`D:\`、`C:\`、`E:/` 六个模式扫描，命中均为 0；未检出真实 Key、账号、tailnet 地址或私有绝对路径。
- **链接与格式：** 文档 133 行；未检出文件行尾空格。第 7 节刻意使用**纯路径文本**而非 Markdown 链接（该文档要整份转发到仓库之外，相对链接在接收方会失效），因此本文档不产生需要解析的相对链接；`docs/LLY/` 其余文档链接仍全部可解析。
- **工作区：** 本次改动含 4 个文件——交付要求文档（新增）、本行动文档（新增）、`docs/LLY/README.md` 与 `docs/LLY/03-progress/PROGRESS_LOG.md`（修改）。
- **未运行的检查：** 未创建任何源码目录，未编写产品代码，未安装 Docker，未读取真实 Key，未发起任何上游请求，未运行产品测试。本行动只产出交付文档，运行测试不适用；**不得以交付文档存在冒充任何拓扑或限额已验证**。
- **遗留与风险：**
  1. 文档第 1 节第 7 项仍待负责人选择；未选定前账本算法不能定稿。
  2. 第 3.2 节的拓扑可行性未知，只能由负责人机器实证回答；若不可行则按停止条件停在本任务。
  3. 第 1 节额度初值来自 2026-09-17 的价格快照，正式使用前须重新核验官方价格与账户资格。
  4. 本文与设计冻结底稿、测试设计、组长机器预案存在内容交叠（均涉及拓扑与私有文件）；本次按"机制/测试/执行/交付四个视角，权威正文只此一份"处理。若后续出现不一致，以权威文档为准并同步四份。
  5. 本次未提交、未推送 Git。
