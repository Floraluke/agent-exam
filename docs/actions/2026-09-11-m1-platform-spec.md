# M1 平台规格与验收整理

## 状态与情况说明

- 状态：Completed（2026-09-11）；用户已确认验收方式，已使用 `to-spec` 发布规格并完成文档验证。本行动结束时正式任务单待粒度确认；此后用户已确认 14 项拆分及文档目录例外，发布进展见新的[任务发布行动](2026-09-11-m1-ticket-publication.md)。下文拆分草案保留为当时快照，不代表当前仍待确认。
- 本次行动独立于已完成的[本地工作流配置](2026-09-10-local-spec-workflow-setup.md)，不复用旧 M0 或配置行动记录。
- 已有成果：M0 真实单题核心闭环通过；本次未重跑历史实验。当前源代码只有本机原型与执行/判卷适配，M1 仍未实现。
- 用户确认：以 HTTP Interface 测提交、所有者批准、排队执行和结果查询；少量浏览器整流程测试检查页面接线；日常采用可控执行/判卷替身，PostgreSQL/MinIO 单独集成验证，最终真实闭环另行授权。
- 配置检查点：`bee4d6c` 已在本地提交，仅含已确认配置及配置行动记录；没有推送。现有其他文档增量保留。
- 本次只发布规格，不修改业务实现、不运行模型、Docker/网络探针、数据库迁移或机器配置；不会把测试替身结果作为正式评测结果。

## 权威依据与已有实现

- [总架构第 3.1 节](../architecture/ARCHITECTURE.md#31-m1-交付边界2026-09-09-已确认)：M1 范围及 Judge/Human Review 延期。
- [模块契约](../architecture/MODULE_CONTRACTS.md)：依赖方向、既有 Module 与 Interface、M0 子集差异。
- [HTTP 契约](../interfaces/HTTP_API.md)、[数据模型](../architecture/DATA_MODEL.md)：用户可观察行为、状态、权限、存储与验证要求。
- [远程接入](../operations/REMOTE_TEAM_ACCESS.md)、[Harbor 验收对账](../interfaces/HARBOR_EXECUTION.md#暂停后的验收对账2026-09-08)、[认证边界](../interfaces/CODEX_AUTHENTICATION.md)：最终验收门槛、已有证据与当前授权的区别。
- [原型入口](../../apps/backend/prototype_codex_harbor_e2e.py)、[ExecutionBackend](../../apps/backend/src/eval_platform/application/ports/execution.py)、[PatchEvaluator](../../apps/backend/src/eval_platform/application/ports/evaluator.py)：复用已有执行和独立判卷；原型不承担网页、数据库、批准或排行。
- [既有契约测试](../../apps/backend/tests/contract/test_m0_pipeline.py)、[既有无模型集成测试](../../apps/backend/tests/integration/test_m0_pipeline_integration.py)：替身注入、身份/哈希校验、故障不冒充结果及重型测试门禁的先例。

上述源代码和 M1 HTTP、数据、模块、远程文档本轮已实际阅读；完整 M0 恢复阅读沿用前轮记录，本次不重新验证私有运行证据。

## 实施措施

1. 已按确认的验收方式写成 52 条连续编号用户故事；同一业务事实的权威来源仍是上述专题文档，没有另设冲突规则。
2. 已用 `to-spec` 发布[本地 M1 规格](../../.scratch/m1-platform/spec.md)，保留七个规定章节和 `ready-for-agent` 标签；标签不授权真实执行或绕过后续设计确认。
3. 已区分日常软件测试、真实存储集成、真实执行/双机验收；所有未执行项目仍为未验证。
4. 已标明账号物理存储、有限参数、现有执行 Interface 生命周期扩展及排行统计口径的实施前检查点，没有新建业务 Module、Interface 或表。
5. 已更新交接入口并完成文档检查；对话中展示 `to-tickets` 任务粒度草案，未提前发布任务文件。规格与交接尚未提交，待拆分确认后核对下一本地检查点。

## 实际修改的文件树

```text
E:/9.1agent_exam/
├─ .scratch/                         # 已批准的本地规格/任务目录，本次首次使用
│  └─ m1-platform/                   # Codex 平台 M1 功能范围
│     └─ spec.md                     # 新增：用户故事、实施约束、验收方式与排除项
├─ HANDOFF.md                        # 修改：配置检查点、规格入口与后续待确认任务
└─ docs/                             # 既有文档目录
   └─ actions/                      # 既有行动记录目录
      └─ 2026-09-11-m1-platform-spec.md  # 新增：本次规格行动及实际验证
```

沿用既有 Adapter 模式：生产和测试的 Execution Backend、Patch Evaluator 均满足各自既有 Interface；主要测试入口为规划中的 HTTP Delivery。测试设计使用 `codebase-design` 的 Interface/Seam 纪律，只替换外部依赖，不把应用用例、状态规则和权限判断全部模拟掉。本次无代码变更或新程序设计模式。

## 修改后自验证方式

- PowerShell 检查规格的七个规定章节、连续编号用户故事、`Status: ready-for-agent`、批准/取消/错误/证据/排行/远程及延期范围均被覆盖。
- 对照权威文档检查故事和验收：批准不执行、拒绝不可领取、单机并发 1、未知不写 0、基础设施错误不写成题目失败、内部测试不进正式报告与排行、M1 无 Judge/Review 依赖。
- 检查本行动与交接的 Markdown 链接/锚点、围栏、行末空格；执行 `git diff --check`。
- 与本次规格行动开始时的已跟踪文件内容基线对比，只允许本文件树范围发生新变化；不把旧文档增量混入规格检查点。
- 本次不运行业务测试；规格发布表示验收目标已记录，不表示对应功能已实现或通过验收。

## 自验证情况

- 规格结构检查通过：七个规定章节各一处、52 条连续编号用户故事、标签为 `ready-for-agent`，没有具体实现文件路径或代码片段。
- 人工逐项对照 HTTP、数据及模块文档：保留批准与执行分离、单机并发 1、取消/中断语义、三类结果区分、不可变证据、输出保护、限额与保留、内部测试排除、基础排行和私有双机验收；Judge/Review 均明确延期，未实现细节保持待定。
- PowerShell 文档检查通过：规格、交接及本记录共 3 份文档，96 个本地链接、12 个锚点可解析，围栏与行末空格无错误。规定范围与安全标记检查通过；`git diff --check` 通过。
- 规格行动前后对比 130 个已跟踪文件：只有计划内 `HANDOFF.md` 发生新变化，其余 129 个保持原内容；新增规格及本记录。没有修改业务代码、配置检查点内容或既有无关文档。
- 本次没有运行业务测试、真实模型、Docker/网络探针或迁移；没有推送。M1 业务实现、真实存储集成与最终真实验收均未因此完成。

## 后续拆分的待确认事项

- 拟以 14 个可独立验证的纵向任务覆盖身份、任务目录、提交、批准、执行、进度、证据、取消、中断、排行、清理及两层真实验收；详细粒度与依赖先在对话确认，尚未生成任务单。
- `to-tickets` 默认一任务一文件、在同一功能的 `issues/` 下编号。这一草案需要申请仅限该任务文档目录的 14 文件例外，尚未批准、尚未创建；源代码目录仍遵守原有指标。
- 例外理由：保持同一 M1 规格的连续任务编号与依赖引用，避免另造跨目录追踪规则。风险是目录条目增多，但均为短 Markdown，不引入程序依赖；拆成两个功能目录会把单一 M1 计划人为分开，因此先请用户确认这个有界文档例外，而不擅自采用。
