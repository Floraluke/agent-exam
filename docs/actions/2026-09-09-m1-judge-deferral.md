# M1 延后 Judge 与其人工复核：范围同步行动记录

## 1. 状态与情况说明

- 本次行动：已完成，2026-09-09；仅完成已确认范围的文档同步，不代表 M1 实施或验收完成。
- 来源：用户明确同意“Judge 及针对 Judge 的人工复核移到下一阶段，M1 先交付真实可用的 Codex 评测平台”。
- 本次决定只调整交付阶段，不删除后续设计，不改变确定性判卷、所有者批准、证据查看或安全要求。当前阶段范围唯一维护在[总架构](../architecture/ARCHITECTURE.md#31-m1-交付边界2026-09-09-已确认)，其余文档引用该处并说明各自接口/数据的阶段行为。
- 后续指令：用户随后明确要求完成行动文档后使用 Spec 技能分任务、逐步开发 M1，并在关键节点本地提交。本机业务开发已获授权；这不把旧 M0 出口提案整体批准，也不授予 D1/D2 真实运行风险豁免或 B1/B2 Docker 探针许可。
- 现状：M0 单题核心闭环已有历史通过证据，完整验收未结束；M1 未实现。源码和数据库没有因本次确认而具备新能力。
- 本次文档行动排除：不读真实凭据、不运行模型、Docker、网络或业务测试，不改实现/个人技能/机器设置，不推送，不重写历次实验记录。用户新增的开发与本地提交要求将在后续独立行动中执行，提交时不混入无关修改。
- 记录粒度：为这一次范围同步独立新建本文件；旧出口提案只加局部被替代的导航注记，不追加本次实施日志。下一次开发或验证行动另建文档。

## 2. 实施措施

1. 在总架构建立已确认阶段边界，标注规划图、文件树与验收中的后续 Judge/Review 部分。
2. 同步模块依赖、HTTP 路由/兼容字段、数据表/状态和验收阶段；保留后续契约，不为 M1 预建未启用的分析模块或数据表。
3. 同步框架/远程验收与恢复入口，保留所有者批准、结果安全与既有 M0 门槛；旧提案只注明对应范围条目已被新决定替代。
   执行中的范围补充：HANDOFF 同时记录用户新授权的 Spec → 分任务 → M1 本机开发 → 关键节点本地提交顺序；它不是恢复旧真实运行许可。
4. 检查阶段口径、所有本地链接/锚点、Markdown 格式和修改范围，记录真实结果。

完成标准：从架构、接口、数据模型或 HANDOFF 进入，都能分清 M1 必交付与后续分析；不会把复核延期误读为免批准，也不会把范围确认误读为运行授权。

## 3. 受影响文件树

```text
HANDOFF.md                                             # 修改：恢复入口、当前范围与行动记录导航
docs/                                                  # 既有项目文档目录
├─ architecture/                                       # 架构与内部数据契约
│  ├─ ARCHITECTURE.md                                  # 修改：阶段边界权威来源、图与目标树标注
│  ├─ MODULE_CONTRACTS.md                              # 修改：M1 依赖与后续模块阶段
│  └─ DATA_MODEL.md                                    # 修改：表/状态/验证阶段；保留原图中文标注
├─ interfaces/                                         # 外部与交付接口
│  ├─ HTTP_API.md                                      # 修改：M1 路由与空分析字段、后续复核契约
│  ├─ HARBOR_EXECUTION.md                              # 修改：M1 验收引用新范围
│  └─ FRAMEWORK_INTERFACES.md                          # 修改：总览中的后续能力阶段
├─ operations/                                         # 运维验收
│  └─ REMOTE_TEAM_ACCESS.md                            # 修改：M1 远程验收不要求后续复核入口
└─ actions/                                            # 逐次行动记录
   ├─ 2026-09-09-m0-to-m1-exit-gate.md                  # 修改：仅局部被替代注记，保留原提案与未决项
   └─ 2026-09-09-m1-judge-deferral.md                   # 新增：本次范围、措施与验证记录
```

不新增业务模块、接口、表或目录，不改变 Ports and Adapters 的依赖方向。现有扁平行动目录已超过默认文件数量指标；本次按用户确认的逐次新建记录方式新增一份，不迁移或拆分旧文档。目录分层整理另议，避免为范围同步引入无关路径变动。

## 4. 自验证方式

1. 人工对照：所有者批准/两角色/单并发/取消/存储/报告/秘密保护保留；Judge、其人工复核、相关专用表/路由/验收明确后续启用；M0 例外未扩大。
2. PowerShell 检查本轮全部 Markdown 文件的本地链接、锚点、围栏与行尾空白；标题调整保留既有锚点。
3. `git diff --check`，并单独检查尚未跟踪文件；对照修改前 SHA-256 清单，要求本轮只改变第 3 节列出的路径。
4. 核对数据模型原有 Mermaid 图和无关中文标注未被改写，确认业务源码、AGENTS、技能和旧 M0 实验行动记录不变。
5. 本轮不做业务或运行验证；文档检查不能称为 M1 功能验收通过。

## 5. 自验证情况

- 人工核对通过：Owner Approval、证据查看和各安全底线保留；Judge/复核的模块、表、状态、路由、工作台及验收均已明确为后续，目标设计保留。M1 API 的兼容字段为空，不伪造待复核状态。
- PowerShell：本轮 10 份 Markdown 的 218 个本地链接、73 个锚点全部有效；围栏和行尾空白检查无错误。`git diff --check` 退出 0，仅有既有 LF/CRLF 提示；未跟踪文档另由 PowerShell 检查覆盖。
- 修改范围：129 份修改前已有文件的 SHA-256 对照中，只有计划内 9 份既有文档变化，其他 120 份不变；另新增本行动记录。源码、AGENTS、技能、旧 M0 实验记录及既有阅读指南等未变，保留其他未提交与未跟踪内容。
- 数据图核对：本轮补丁只在图外添加阶段说明，没有改写 DATA_MODEL 中的 Mermaid 图、英文 ID、状态边或原有中文标注；没有运行图形渲染测试。
- 技能检查：本机找到并完整读取 `to-spec`、`to-tickets`、`implement` 和其前置 `setup-matt-pocock-skills`；项目尚无 tracker 配置，已询问本地 Markdown/GitHub Issues 的选择。按技能要求，配置及测试接口/拆分确认完成后才能发布任务并进入实现；不冒充已经使用 Spec 完成拆分。
- 当前交接：用户已经授权 M1 本机开发和关键节点本地提交，不再重复询问总授权；目前等待技能配置选择，不是业务实现失败。下一次配置/规格/开发行动独立记录，按实际完成节点提交，不把无关增量混入。
- 本轮未运行业务测试、模型、Docker 或网络探针，未使用真实凭据、修改机器设置、提交或推送。权威文档状态已同步；本次行动记录不再承载后续 M1 开发流水。
