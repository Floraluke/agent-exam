# 任务 05 待负责人决定与授权的第二轮回执文档

## 状态与情况

- 状态：已完成。
- 来源请求：用户将合并本次提交，并要求把"需要从负责人那边拿到的、让对方执行的"写清楚，以便直接转发给对方；拿到对方结果后即开工。
- 当前事实（已核对）：
  - 工作区干净、无未推送提交（`af00f83` 已在 `origin/lly/dev`），因此本次无待提交内容需要先处理。
  - 负责人上一轮只读核对的结论为 **STOP**：6 项前置仅 1 项完整满足、7 条断言全部未执行、**9 项决定全部仍为"待负责人确认"**（第 7 项推荐 A 保守上界起步但未作选择）。
  - 因此当前唯一的阻塞是**负责人侧的两件事**：① 拍板 9 项数值；② 授权拓扑实证的机器窗口与资源范围。二者均非 E 可代决。
  - 上一轮负责人指出的两点已处理完毕：断链已补交修复、前置措辞缺陷已修正。
- 已确认决定：本次只新增一份**面向负责人的第二轮回执文档**，采用"自足、可直接转发、含可回填表格与可直接批准的授权范围"的形态；不实现任务 05、不创建容器或网络、不向负责人索取真实凭据。
- 明确排除：不修改产品代码，不新建 Module/Interface/表，不代负责人拍板任何数值，不把"待确认"写成"已确认"。

## 实施措施

1. 把 9 项决定做成一列待填表格，并提供"全部同意建议"这一最小回执方式，降低对方的回复成本。
2. 把授权需求具体化到**可批准的程度**：给出建议的项目名、逻辑网络名、服务名、标签与清理规则，使负责人能直接批准或修改，而不是面对抽象请求。
3. 补上上一轮前置核对中三项需要负责人处理的事项，并明确其中一项（假 Key 文件）**不需要对方提供**——由探针在 Git 忽略的证据目录内自行生成并自检权限，减少对方负担。
4. 明确列出探针**不会做**的事（不改共享 Docker/WSL/代理/防火墙、不碰既有容器与持久化服务、不接公网、不下载镜像、不用真实 Key），使授权范围可被知情批准。
5. 写出"拿到回执后 E 侧的开工顺序"，并标明哪一步不必等拓扑结论、哪一步必须等，便于双方排期。

完成标准：负责人无需阅读其他文档即可作出决定与授权；授权范围含明确的资源命名、操作边界与清理规则；9 项决定保留为待填；不出现真实凭据或私有绝对路径；不声称任何前置或断言已通过。

## 受影响文件树

```text
docs/LLY/01-plan/
└─ TASK05_OWNER_ACTION_REQUIRED.md   # 新增：第二轮回执文档（9 项决定表 + 可直接批准的授权范围 + 开工顺序）
docs/LLY/
├─ README.md                        # 修改：01-plan 补充该文件入口
└─ 03-progress/PROGRESS_LOG.md      # 修改：追加本次记录与当前停点
docs/actions/
└─ 2026-09-21-task05-owner-round2.md # 新增：本行动记录
```

设计关系：本行动只新增一份交接文档，不创建源码目录、不新增 Module/Interface/表，不改变模块边界与依赖方向。文档与既有的交付要求、负责人填充版构成"请求 → 回复 → 回执"三段配对，不复制权威正文。

## 自验证方式

1. 自足性检查：确认文档包含作出决定与授权所需的全部信息与回填位置，不要求读者先读其他文档。
2. 授权可批准性检查：确认给出的资源命名（项目名、网络名、服务名、标签）、操作边界与清理规则具体到可直接批准或就地修改。
3. 数值归属检查：确认 9 项仍为待填或待确认，未被本文件代为设定；"全部同意建议"只是最小回执方式，不改变待确认性质。
4. 负面清单检查：确认写明不改共享 Docker/WSL/全局代理/防火墙、不碰既有容器与持久化服务、不接公网、不下载镜像、不使用真实 Key、不全局 prune。
5. 负担减轻检查：确认明确写出假 Key 文件由探针自行生成、不需要负责人提供。
6. 敏感值扫描：确认不含真实 Key、真实账号、tailnet 地址、私有绝对路径。
7. 链接与格式：文档内相对链接可解析、代码围栏成对、无行尾空格；`git status` 只出现本次预期文件。
8. 不运行产品测试：本行动只产出交接文档，运行测试不适用；不得以文档存在冒充任何前置或断言已通过。

## 自验证情况

- **自足性检查通过：** 文档含作出决定与授权所需的全部信息与回填位置——9 项决定表（第 4 列待填）、四项前置的处理方式、探针会创建/删除的确切范围、不会做的事、停止条件、授权回执模板、开工顺序。正文不要求读者先读其他文档；上一轮两份文件仅在文首作为配对链接出现。
- **授权可批准性检查通过：** 给出了 Compose 项目名 `agentexam-t05-topology`、逻辑网络 `internal`/`egress`、服务 `workload`/`proxy`/`fake-upstream`、标签 `agentexam.task=05` 与 `agentexam.scope=t05-topology-<日期>-<序号>`、卷（不需要）、清理规则（只按名称与标签删除、按同一名称与标签复核残留为 0、禁止全局 prune）。负责人可直接批准或就地改字。
- **数值归属检查通过：** 9 项全部保留为待填或待确认；"建议全部采用"只作为最小回执方式出现，未改变其待确认性质；第 7 项给出 A/B 两条路线的后果但未代为选择。
- **负面清单检查通过：** 第 2.3 节含"不改共享 Docker/WSL、全局代理、防火墙与既有容器""不停止、不删除、不连接既有容器与网络，包括 PostgreSQL/MinIO 持久化服务""不接公网出站""不下载镜像""不使用真实 Key、不读登录文件、不发起真实供应商请求、不充值""不为让断言通过而修改断言或跳过条目"；第 2.5 节另列授权后仍遵守的停止条件。
- **负担减轻检查通过：** 第 2.4 节明确假 Key 文件由探针自行生成、不需要负责人提供，并说明默认位置（Git 忽略的证据目录）与"若要求放在仓库目录树之外可按路径参数执行"两种方式；同时说明拒绝用例本就需刻意造出符号链接、宽权限、共享目录等错误形态，由探针自造最可控。
- **敏感值扫描：** 对 `61A17959`、`sss.tail03c757`、`sk-`、`D:\`、`C:\`、`E:/` 六个模式扫描，命中均为 0；未检出真实 Key、账号、tailnet 地址或私有绝对路径。
- **链接与格式：** 文档内相对链接（`TASK05_OWNER_DELIVERY.md`、`TASK05_OWNER_DELIVERY_FILLED.md`）指向同目录文件，均可解析；`docs/LLY/` 全部链接可解析；未检出文件行尾空格。
- **工作区：** 本次改动含 3 个文件——回执文档（新增）、本行动文档（新增）、`docs/LLY/README.md` 与 `docs/LLY/03-progress/PROGRESS_LOG.md`（修改），共 4 个路径。
- **未运行的检查：** 未创建容器、网络或卷，未运行断言，未创建假 Key，未读取真实凭据，未运行产品测试。本行动只产出交接文档；**不得以文档存在冒充任何前置或断言已通过**——当前仍是 1/6 前置满足、7 条断言未执行。
- **遗留与风险：**
  1. 任务 05 仍为**未发布、未授权**状态；本文档不改变这一点，只把待办整理成可回执形态。
  2. 两处阻塞均在负责人侧（9 项决定、授权范围），文件不构成对方的决定；在收到回执前不得视为已授权。
  3. 若负责人修改建议的资源命名或标签范围，第 2.2 节与拓扑探针须按修改后范围同步，不能沿用本文建议值。
  4. 第 3 节开工顺序中的第 2–4 步虽声明不必等拓扑结论，但仍需先取得 9 项决定（尤其第 7 项）与本任务的形式授权；不能据此把"并行开工"理解为已获授权。
  5. 本次未提交、未推送 Git。
