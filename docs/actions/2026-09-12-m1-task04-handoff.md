# 任务 04 已确认方案的窗口交接

## 状态与情况说明

- 状态：Completed。已记录用户对任务 04 方案的确认及换窗口要求，根交接已提供下一窗口恢复步骤与完整提示词；未开展任务 04 实施。
- 本行动是独立的交接整理，不是任务 04 实施。业务计划继续由[任务 04 行动](2026-09-12-m1-job-submission.md)维护，方案确认及下一窗口恢复顺序记录于[根交接第 6 节](../../HANDOFF.md#6-下一窗口的工作顺序与验收)。
- 只修改根 HANDOFF.md 和本记录；任务单、实现行动、架构/数据/接口等的批准状态同步留给下一窗口第一步。不修改业务代码，不运行业务测试、容器或模型，不提交、不推送。

## 实施措施

1. 读取既有交接、任务 04 行动/任务单及本地 Git 状态；核实最后提交和未完成事项。
2. 在根交接明确用户已确认、当前是主动换窗口而非等待批准；保留已完成任务、旧证据和混合改动的事实源指针。
3. 更新第 6 节执行顺序和第 7 节可粘贴提示词：恢复必读、先同步确认状态、直接按已批准方案实施任务 04、完成评审修复后停止。
4. 只作 Markdown/链接/锚点/diff 和修改范围验证，记录真实结果。

## 需要修改的文件树

```text
E:/9.1agent_exam/
├─ HANDOFF.md                                      # 修改：唯一恢复入口、授权变化和下一窗口提示词
└─ docs/actions/2026-09-12-m1-task04-handoff.md      # 新增：本次独立交接的范围与验证记录
```

不新增业务 Module、Interface、数据库表或实现目录，不改变设计模式。

## 自验证方式与成功标准

- 两份文档本地链接及锚点有效，代码围栏闭合，无尾空白；git diff --check 通过。
- 根交接第 6、7 节明确方案已确认；任务 04 未实现、验收未跑、下一窗口恢复后不重复索取已有授权。
- 其他既有变更保持不动；暂存区为空，HEAD 保持 3274e18；本轮只读 Git，不联网核验远端或 Docker。

## 自验证情况

- 两份文档的 123 条本地链接、13 处锚点、围栏、空白与 git diff --check 均通过；第 6、7 节的确认状态、停止点、固定评审基准和恢复步骤已逐项核对。
- 既有 12 个其他已修改文件的 SHA-256 前后相同，业务源码无差异；HEAD 仍为 3274e18，暂存区为空。本轮仅修改 HANDOFF.md 并新增本记录，没有提交、推送、容器或业务测试。
- 首次采集 Git 文件哈希时，Git 的非致命告警混入 JSON 导致读取失败；随后隔离告警并检查退出码，成功取得和复核快照。属于检查输出解析问题，不是项目故障。
- 按 handoff / writing-for-agents 保留专题事实源链接，把当前恢复步骤集中在前；按项目既有入口维护根 HANDOFF，不另建临时副本。任务 04 待批旧标签的回填明确交由下一窗口执行，避免超出本轮仅交接范围。
