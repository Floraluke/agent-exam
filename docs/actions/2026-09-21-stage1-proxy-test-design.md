# 阶段 1（05）假提供方安全执行链测试设计准备

## 状态与情况

- 状态：已完成。
- 来源请求：用户询问 E 模块有无可提前开展的工作，随后同意"先做把负例矩阵转成 05 可执行测试清单"这一项。
- 当前事实（已实际核对）：
  - 05（假提供方安全执行链）是 E 的主责任务，24 h，**尚未发布独立 issue、未获实施授权**；03–08 均未发布。
  - 按 `TEAM_WORK_ALLOCATION.md` 第 2 条第 7 款，未发布期间允许"提前阅读自己 Module 和准备测试设计"，不允许提前修改后续任务代码。本行动只做后者。
  - 本机（`D:\agent-exam`）按阶段 0 决定**不安装 Docker**，且 `framework/`、`runtime/`（Harbor/Fork/镜像）不进 Git、只在组长机器上。因此涉及容器与网络隔离的验证在本机无法运行。
  - `apps/backend/tests/providers/` **当前不存在**，是 `implementation-map.md` 第 3 节列的候选测试目录。
  - 现有显式开关约定为 `AGENTEXAM_RUN_<区域>=1`，共 13 个已在使用；现有分层验证入口为 `tests/catalog/runtime/verify.ps1` 与 `tests/jobs/runtime/verify.ps1`。
  - 05 的代理实现（`adapters/execution/provider_access/`、`codex/provider_config.py`）**当前均不存在**，属候选新增。
- 已确认决定：本次只产出**测试设计文档**，不创建 `tests/providers/`、不写任何测试代码、不修改产品源码。已知的负例清单与安全候选不在本文复制，只链接权威来源。
- 明确排除：不修改产品代码，不新建 Module/Interface/表/源码目录，不安装 Docker，不调用真实模型，不下载 Harbor/Fork/题目镜像，不读取真实 Key，不提交或推送 Git。

## 实施措施

1. 读权威来源：`verification.md` 第 4 节"代理与限额"负例矩阵、`implementation-map.md` 第 2/3/4/5 节、`CODEX_AUTHENTICATION.md` 第 4.1/5 节、`research/2026-09-17-codex-provider-config-and-budget.md` 第 2–5 与 6.1 节、`plan.md` 第 7 节。
2. 核实现有测试布局、显式开关命名、分层验证入口脚本模式，使设计沿用现有约定而非另造一套。
3. 把负例矩阵逐条映射为：测试文件路径、用例名、前置构造、断言、以及**本机 / 组长机器**的可运行归属。
4. 标出 05 开工时必须先冻结、现在不得预设的技术未知，避免测试设计变成对未验证事实的假设。
5. 记录前置条件与风险，特别是"第一步必须在组长机器"对本机开发的排期影响。

完成标准：五组负例每条都有明确归属（测试文件 + 用例 + 断言 + 运行位置）；本机可跑与必须到组长机器的部分界限清楚；文中不含任何"已实现""已验证"的表述；权威正文未被复制，只以链接引用。

## 受影响文件树

```text
docs/LLY/01-plan/
└─ STAGE1_PROXY_TEST_DESIGN.md      # 新增：05 负例矩阵到可执行测试的映射、分层、运行位置与待冻结未知
docs/LLY/
├─ README.md                        # 修改：01-plan 分类说明补充该文件入口
└─ 03-progress/PROGRESS_LOG.md      # 修改：追加 2026-09-21 测试设计准备记录
docs/actions/
└─ 2026-09-21-stage1-proxy-test-design.md   # 新增：本行动记录
```

设计关系：本行动只做**测试设计**，不创建测试或实现文件，因此不涉及设计模式，也不改变模块边界、依赖方向或任何 Interface。文档中的测试文件路径是**候选规划**，与 `implementation-map.md` 第 3 节的候选树保持一致；未获 05 授权前不创建这些目录。

## 自验证方式

1. 逐条比对：`verification.md` 第 4 节"代理与限额"的每条负例在设计文档中都有对应归属，无遗漏；反之设计文档不引入权威来源之外的新验收要求。
2. 检查设计文档未复制权威正文（不包含字段表、路由表、价格表或负例原文的成段搬用）。
3. 检查文中的测试路径、开关名、脚本名与现有约定一致：开关沿用 `AGENTEXAM_RUN_*`，入口沿用 `tests/<区域>/runtime/verify.ps1` 模式。
4. 检查文中所有"本机/组长机器"判断与已知环境事实一致（本机无 Docker、无 `framework/`、无 MinIO）。
5. 检查文档内部链接可解析、代码围栏成对、无行尾空格；`git status` 只出现本次预期文件。
6. 不运行产品测试：本行动不产生可运行代码，运行测试不适用；不得以文档产出冒充测试通过。

## 自验证情况

- **覆盖核对（第一轮发现遗漏，已修复）：** 拿[验证规范第 4 节](../../.scratch/ui-catalog-providers/verification.md)的五组负例逐条对照设计文档，发现 **3 处遗漏**：① 直连供应商/宿主/metadata/其他 Trial；② 容器文件与进程、Docker inspect/patch 的假 Key 探查；③ 新 Job 重试需重新批准。三处均已补充到对应小节。现五组负例全部有明确归属（测试文件 + 用例名 + 断言 + 运行位置）。
- **未复制权威正文：** 抽三处权威原文短语检索设计文档，命中均为 0——「出站计数0且脱敏错误」、「不静默增额/退回未知用量/重试」、「跨Run令牌、过期/撤销令牌」。
- **约定一致性：** 显式开关沿用现有 `AGENTEXAM_RUN_<区域>=1` 形态（现有 13 个在用）；分层入口沿用 `tests/<区域>/runtime/verify.ps1` 模式（现有 catalog、jobs 两处）；候选测试目录 `tests/providers/{policy,contract,lifecycle,integration}` 与实现地图第 3 节列的候选树一致。以上均标注为**候选**，需 05 发布时按当时实现确认。
- **环境事实一致性：** 设计中的运行位置判断与本机实测事实一致——本机无 Docker（阶段 0 明确决定）、`framework/`、`runtime/`、固定镜像只在组长机器、MinIO 未配置。因此集成分层（容器拓扑、直连拒绝、宿主隔离、精确清理、容器内假 Key 探查）全部划归组长机器，其余三层留在本机。
- **链接与格式：** `docs/LLY/` 全部仓库内相对链接可解析；Markdown 代码围栏成对；未检出文件行尾空格。
- **工作区：** `git status --short` 只出现本次预期文件——2 份修改的 LLY 文档、2 个新增文件（设计文档与行动文档），无其他改动混入。
- **未运行的检查：** 未创建 `tests/providers/`，未编写任何测试或产品代码，未安装 Docker，未调用模型，未运行产品测试。本行动只产出设计文档，运行测试不适用；**不得以本文档存在冒充任何测试已通过**。
- **遗留与风险：**
  1. 设计中的测试文件路径、用例名、开关名、验证入口脚本名均为**候选**，05 发布时须按当时实现确认；本次不创建这些目录。
  2. 契约层（固定 CLI 配置渲染与请求形状）的运行位置标注为"待确认"，因为研究第 6.1 节的探针边界未说明是否使用容器封装。
  3. 四项必须开工前冻结的未知已在设计文档第 5 节列明；在这些冻结前落测试代码会把假设编码成断言。
  4. 05 的第一步（最小拓扑实证）必须在组长机器，本机无 Docker；这是排期风险，已在设计文档第 6 节记录。
  5. 本次未提交、未推送 Git。
