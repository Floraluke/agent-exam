# 正式登录、Worker 运行与交接同步

## 状态与情况说明

- 状态：已完成。
- 来源：用户要求启动真实 Worker，完成后要求更新 `HANDOFF.md`。
- 范围：把 2026-09-21 已实际发生的正式登录恢复、FastAPI Origin 修正、真实 Worker 启动、完成 Job 及当前运行状态同步到交接入口。
- 已确认事实：正式库唯一 `owner` 账号处于启用状态；密码已通过本机恢复流程重置但不得写入仓库；本机和公开 HTTPS 登录均返回 200 并签发安全会话 Cookie；真实 Worker 生产装配预检通过；Job `d62cd6ea-a342-4a70-90f2-e5ab4afb115f` 的 Job/Run 均为 `COMPLETED`；2026-09-21 17:43:42 +08:00 快照中 Worker PID 34852 仍运行，停止标记不存在。
- 凭据边界：只记录现有 `auth.json` 已由生产预检校验并被获准的 Worker 复用；不记录文件路径之外的内容，不记录 owner 密码、数据库密码、MinIO 密码、会话 Cookie 或 Token。
- 明确排除：不修改源码、配置、数据库结构或业务数据；不启动任务 04–08；不提交或推送；不把进程 PID 或即时队列状态描述为跨重启保证。

## 实施措施

1. 新增本行动记录，保存本轮变更范围、事实证据和保密边界。
2. 最小更新 `HANDOFF.md` 顶部恢复摘要、当前阶段、授权边界和第 6 节运行快照，消除“Worker/模型未启动”等已失效的当前状态描述。
3. 历史记录继续保留为历史，但为可能与当前快照冲突的句子加上时间语境或指向最新运行快照。
4. 检查 Markdown 差异、空白错误、链接目标和敏感信息；核对行动记录与交接入口描述一致。

完成标准：接手者无需查看本轮聊天即可知道正式 Web 登录已恢复、Worker 是否在快照时运行、哪条 Job 已完成、记录到的用量值、如何安全停止，以及任务 04–08 仍未获自动实施授权；仓库中不出现任何明文凭据。

## 受影响文件树

```text
HANDOFF.md
  # 当前恢复入口；更新正式登录、运行态 Worker、完成 Job、授权边界和接续步骤。
docs/actions/2026-09-21-live-runtime-handoff.md
  # 本轮文档变更的可追踪执行记录、证据、排除项和验证结果。
```

本轮不涉及设计模式、业务 Module、Interface、数据库表或目录结构变更；`HANDOFF.md` 只引用现有 owner-host runtime 和任务 03 事实源。

## 修改后自验证方式

1. `git diff --check -- HANDOFF.md docs/actions/2026-09-21-live-runtime-handoff.md`：无空白错误。
2. `rg -n 'Worker 与模型未启动|未读取.*auth\.json|未调用模型|未运行真实 Worker|未发起模型请求|03 等待用户确认 UI' HANDOFF.md`：不存在被当作当前状态的失效断言；允许带明确“历史”语境的记录。
3. `rg -n -i 'password|密码|auth\.json|cookie|token' HANDOFF.md docs/actions/2026-09-21-live-runtime-handoff.md`：人工复核所有凭据相关行，不得出现 owner 明文密码、私有密码正文、会话值或认证正文。
4. 检查新增相对链接目标均存在，并重新读取 `HANDOFF.md` 顶部、第 1/2 节和第 6 节，确认与本行动记录一致。

## 自验证情况

- `git diff --check -- HANDOFF.md`：通过；Git 仅提示当前工作副本未来可能从 LF 转为 CRLF，不是空白错误。新增行动文档另行检查行尾空白，结果为 `ACTION_WHITESPACE_OK`。
- 失效当前态检索：修正后的 `rg` 检查返回 `STALE_CURRENT_ASSERTIONS_NONE`；“Worker/模型未启动”“03 等待用户确认 UI”等不再作为当前事实出现。历史段落保留早期停点，并明确标为历史或指向最新快照。
- 凭据相关行人工复核：只包含 `auth.json` 文件名、Cookie 名称、密码不得落盘等边界说明；未出现 owner 明文密码、数据库/MinIO 密码、Cookie 值、Token 或认证正文。
- 链接目标检查：本行动记录、owner-host runtime 行动指南、任务 02/03 行动及 Harbor 接口目标均存在。
- 回读检查：`HANDOFF.md` 顶部、第 1/2 节和第 6 节一致记录任务 01–03 完成、正式 HTTPS 登录恢复、真实 Worker 当前授权、完成 Job/Run、真实用量值和任务 04–08 不自动继续；进程 PID 与队列明确标为 2026-09-21 17:43:42 +08:00 的瞬时快照。
- 变更范围：实际只修改 `HANDOFF.md` 并新增本行动记录；未改源码、配置、数据库结构或业务数据，未提交或推送。文档专用变更未运行代码测试。
- 遗留风险：`infra/.env` 的正式私有占位符与行动指南中的简单导入片段存在引号/空值处理差异；本轮运行以仅存在于进程内的安全组装规避，并在交接中记录，未在用户仅要求更新 HANDOFF 的范围内修改启动脚本或权威行动指南。后续若固化启动入口，应单独建行动并做无凭据红绿验证。
