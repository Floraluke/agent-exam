# 全量核心代码诊断审查行动

## 状态与情况说明

- 状态：已完成；本文件自此作为历史行动档案只读。
- 来源：用户要求使用 `code-review-skill` 审查当前项目全部核心代码，核对实现与权威文档，并输出包含问题定位、模块定位、修复方向和预计工时的诊断报告。
- 范围：Python 后端、Next.js/TypeScript 前端、PowerShell/Docker 本地基础设施、自动化测试，以及仍需随代码保持一致的架构、接口和运维权威文档。
- 已确认决定：本轮是审查阶段，不修改业务代码；允许为标准测量编写测试，但只有发现现有测试不足且确有必要时才新增；用户另行明确授权把文档归档边界写入 Codex 项目协作规则。
- 明确排除：既有 `docs/actions/` 行动记录和 `docs/research/` 研究文档是历史事实档案，本轮不修改，也不以当前代码反向改写历史结论；用户现有未跟踪文件不修改。
- 未知项：需通过静态检查、测试和代码—文档映射确认具体问题、严重度及修复工作量。

## 实施措施

1. 冻结并记录审查时点的 Git 状态、代码与权威文档范围。
2. 按语言及跨领域检查表审阅模块边界、正确性、安全、性能、错误处理、并发、可维护性和测试充分性。
3. 运行与风险相称的现有测试、静态检查及文档一致性检查；如现有测量不足，仅在必要时新增诊断测试。
4. 将“既有行动/研究文档作为历史档案只读，其他权威文档仍须随代码同步”的规则写入 `AGENTS.md`。
5. 生成独立诊断报告，逐项给出证据位置、模块、影响、修复方向、预计工时和验证建议。
6. 核对报告、协作规则和本行动记录与实际检查结果一致，并完成本行动记录。

完成标准：报告覆盖全部核心模块和文档一致性，所有结论可定位到文件/行或实际检查输出；未运行、失败、跳过和环境限制均如实标注；业务代码保持未修改。

## 受影响文件树

```text
AGENTS.md
  项目级 Codex 协作规则；新增历史行动/研究档案与持续同步文档的边界。
docs/
├── actions/
│   └── 2026-09-21-core-code-diagnostic-review.md
│       本次审查和文档变更的可追溯执行记录；既有行动记录均不修改。
└── reviews/
    └── 2026-09-21-core-code-diagnostic-report.md
        全量核心代码诊断报告，承载问题、模块、修复方向、工时和验证证据。
```

- 不新增业务 Module、Interface、数据库表或运行时目录。
- 不采用新的设计模式；报告仅描述现有模块及其依赖关系。

## 自验证方式

- `git diff --check`：新增/修改文档无空白错误。
- `git diff -- AGENTS.md docs/actions/2026-09-21-core-code-diagnostic-review.md docs/reviews/2026-09-21-core-code-diagnostic-report.md`：变更范围只包含已授权文档。
- 使用 `rg` 核对归档规则、报告必需字段、文件链接与问题证据路径。
- 后端：按 `pyproject.toml` 实际配置运行 Ruff、Mypy、Pytest；涉及外部基础设施的检查单独标注。
- 前端：按 `package.json` 实际脚本运行 lint/type/build 或等价检查，并运行可用的浏览器测试；依赖外部服务的检查单独标注。
- 基础设施：运行无需改变机器状态的配置和 PowerShell 单元检查；不会因审查自动启动/停止服务或真实 Worker。

成功标准：检查命令实际退出码为 0；任何非 0、跳过或环境门槛在报告与本记录中一致呈现，不把历史结果冒充本轮结果。

## 自验证结果

- 审查基线固定为 `main` / `c71d342041e45c63dad91883b6d4bb33289b0ec3`；审查期间核心代码未变化。另一个任务持续修改交接、规划、部分架构/行动/进度文档和 `.scratch` 草稿，已从稳定文档判断中排除并在报告标明。
- 已生成 `docs/reviews/2026-09-21-core-code-diagnostic-report.md`，包含 10 项诊断；每项均给出问题定位、模块定位、影响、修复方向和预计工时。
- 已按授权更新 `AGENTS.md`：已结束行动文档和已完成研究文档封存为只读历史；架构、接口、运维、状态等当前权威文档继续随代码齐平。
- Ruff lint 通过；Ruff format check 通过 300 个文件。默认无参数 Mypy 因 `py.typed` 目标配置失败；显式检查 `src` 与原型脚本通过 169 个源文件，失败及绕行均已列入报告。
- 后端测试：424 passed、96 skipped、3 warnings，44.72 秒。基础设施测试：11 passed、10 skipped、5 warnings，2.21 秒。根目录 Pytest 会误收集虚拟环境并产生 690 个收集错误，已列为质量门禁问题。
- Web：TypeScript 检查通过，生产构建通过，使用项目支持的系统 Chrome 运行 Playwright 为 43 passed；`npm audit --omit=dev` 为 0 vulnerabilities。
- 未安装 Python `pip-audit` 和 coverage 工具，因此没有把 Python 依赖安全或覆盖率描述为通过。PostgreSQL、MinIO、Docker、Harbor、Fork、真实提供方等环境门控项没有把历史证据冒充本轮结果。
- 本轮未新增测试文件，因为现有测试、补充静态规则扫描和代码路径分析已能形成可定位结论；未修改业务代码。
- 最终文档检查已完成：目标文件无尾随空白或补丁格式错误；报告的“问题定位、模块定位、修复方向、预计工时”字段和 CR-01 至 CR-10 均可检索；业务源码与测试目录没有本轮改动。本行动结束并封存。
