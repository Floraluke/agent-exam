# 任务 05 第三块（`service.py` 与接线）实施计划

## 状态与情况

- 状态：Completed（2026-09-21）。本行动**只新增一份准备件**，不改动产品代码、不运行容器、不调用模型。
- 来源请求：用户"这第三块你写一份完整的实施计划出来我可以开会话单独做"——即该计划要能被一个**新会话**直接执行。
- 已核对的当前事实（写入计划并用于校验）：
  - `provider_access/` 现有 **7 个源文件**（上限 8），`tests/providers/policy/` 已有 **8 个**，因此本片**必须建子目录**才符合指标——这不是架构扩张，是指标要求的解法。
  - 已完成的模块可作为接缝复用：`request_policy`（白名单与出站前拒绝）、`budget`（保守上界账本、原子预留、未知即全额计费并关闭）、`binding`（令牌与 Run 绑定）、`secrets`（私有文件与受控上游）、`transport`（固定上游、结构性不重试）、`failures`（受控文案映射）。
  - 取证工具已就位：契约层假上游（`tests/providers/contract/support/fake_responses.py`）自带请求记录，是"出站计数为 0"的唯一证据来源。
  - 基线数字用于开工前对照：`tests/providers` 109 passed / 1 skipped；默认回归 531 passed / 105 skipped / 2 failed；格式 326 文件；类型 176 源文件。
- 明确排除：不给开工授权、不替代权威文档、不做 T2、不写产品代码。

## 实施措施

1. 核对既有接缝与指标现状（文件数、上限、既有测试目录），确认新增文件必须落在子目录。
2. 把第 3–7 项的载体拆成可顺序执行的分片 S6a–S6e（加 T2 之后的 S10/S11），每片写明"失败用例优先 → 最小实现 → 通过 → 回归"与成功标准。
3. 为每个决策点给出**推荐 + 备选**（HTTP 服务器、出站注入、结算时机、流透传、进程入口），并说明理由与代价。
4. 把取证方式写成不可含糊的规则（出站计数以假上游记录为证、受控文案哨兵扫描、每片区分力实测）。
5. 标明阻塞项与需要另行确认的事项，避免新会话自行扩大范围。

## 受影响文件树

```text
docs/LLY/01-plan/STAGE1_PROXY_SERVICE_PLAN.md   # 新增：第三块实施计划（准备件，136 行）
docs/actions/2026-09-21-task05-service-plan.md  # 本行动记录
docs/LLY/README.md                              # 同步：当前状态加上这份计划的指针
docs/LLY/03-progress/PROGRESS_LOG.md            # 同步：本日进度追加一条
```

## 自验证方式

1. 计划内所有相对链接可解析（同目录/上级目录逐条检查）。
2. 计划引用的既有事实可核对：文件数、基线数字、既有模块名、取证工具路径。
3. 不越权：计划内不写"已授权"，授权与阻塞单列一节。
4. 与权威文档不冲突：机制一律引用设计冻结与执行计划，不复制其正文。

## 自验证情况

- **链接检查**：计划内 8 处相对链接全部可解析（脚本逐条 `os.path.exists`）。
- **事实核对**：`provider_access/` 7 文件、`tests/providers/policy/` 8 个、`tests/providers/contract/support/fake_responses.py` 存在、基线数字取自本日实测（`pytest tests/providers` 109/1、默认回归 531/105/2、格式 326 文件、类型 176 源文件），均已实际运行过。
- **指标合规**：计划中的新增文件全部落在子目录内（`provider_access/server/`、`tests/providers/lifecycle/`），每个目录不超过 8 个文件。
- **未做**：计划未实施；其中的 S10/S11 明确标注等 T2。
