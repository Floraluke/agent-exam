# 2026-09-21 D：08 与 05–07 的 D 侧验证设计（准备件）

## 状态与情况说明

状态：Completed（2026-09-21）。本文是**准备件**：把 D 在 08 与 05–07 里那部分的验证方式先写清楚，不含产品代码改动。

来源请求：用户"所有任务都能做了，只要能做就做"。

许可依据：执行计划第 1 节允许"各成员可以提前阅读自己 Module 和准备测试设计"，同时不允许提前修改后续任务产品代码、不调用真实模型、不下载大体量镜像。本文只做设计。

当前事实（已核对）：

- 08 未发布；其前置是 03、04、06、07 验收（plan 第 10 节）。03 已由 B 收口（Web 对比页接既有只读合同）；04 已由 C 收口（六题入库、双存储用真实 MinIO 真跑）；05 停在"未发布、未授权、前置未满足"（任务单 Comments：9 项拍板待负责人确认、拓扑实证未执行）；06/07 未开工。
- 12 Run 的形态已权威确认：**六题 × 两个新 API 配置 = 12 个 Run**，与 06/07 两个烟测共计划 14 次真实 Run（plan 第 10 节步骤 1）；C 亦答复维持"六题 + 两配置"、不加第三配置。
- D 侧已就位的可预演证据：十二 Run 演练（本仓库 `tests/jobs/reporting/test_matrix_rehearsal_twelve_runs.py`）、五档与缺失语义、恢复/重试 `tests/jobs/recovery/` 16 个用例、旧 Job 快照读回、制品/轨迹读取路径、规模边界 `tests/jobs/scale/`。

明确排除：不改产品代码、不改他人负责的文档、不真实调用、不拉镜像；本文不构成 08 或 05 的完成声明。

## 一、08 的 D 侧验证设计

| 计划步骤 | D 的验证动作 | 证据形态 | 现在状态 |
|---|---|---|---|
| 1. 12 矩阵 Run + 2 烟测 = 14 次真实 Run，一次尝试、零自动重试、并发 1 | 每个 Job 的 Run 数等于"题目 × 配置"笛卡尔积；`rerun_of_job_id` 为空；Run 事件序列里没有第二次尝试；同一时刻只有一个重型 Job 被领取 | Job/Run 行、状态事件、对比矩阵每格的来源 Job/Run | 形状已预演（十二 Run 演练，真实 PG + 受控假执行）；真实数据待 06/07 |
| 2. 预算分配与账额 | 逐 Run 记录"实际可获得的用量与费用"，缺值保持 `null`、不写 0；账额观察只在 owner 私有通道，不进入 HTTP/DB/制品 | 报告 `process_metrics.usage`、运行记录表 | 结构已就位；数字与复核由负责人按研究文档执行，D 不复制预算数字 |
| 3. 全流程提交 → 批准 → 报告，逐 Run 记录 | 用 D 的 08 记录模板逐格填写：终态、确定性结果（含 FAIL_TO_PASS/PASS_TO_PASS）、可获得用量与费用、基础设施失败码、证据引用、清理确认 | 08 行动文档的记录表 + 对比报告 | 模板已就位（`2026-09-19-d-module-preparation.md` 的"逐 Run 记录"骨架） |
| 4. 执行验证清单（默认后端、Web 类型/构建、浏览器、隔离真实存储、代理与固定 Fork 回归、旧 ChatGPT 合成链、旧快照与新数据） | 后端与存储项由 D 跑并记录实际命令与输出；Web/浏览器项归 B，代理与 Fork 项归 E，D 引用其结论不代跑 | 逐项命令 + 通过/失败/跳过/延期 | 后端项本机可跑；其余待各自窗口 |
| 5. 双轴评审与逐项结论 | 产品功能、真实提供方接入、M1-14、长期部署**分别下结论**；矩阵分母与未完成分开；不得靠"跳过基础设施失败"宣称全通过 | 08 收口的逐项结论表 | 待 08 发布 |

验收口径（plan 第 10 节原文）：矩阵每项有证据和明确结论；12 个矩阵 Run 不必全解对题，但不能靠跳过基础设施失败宣称全通过；统计分母与未完成分开。

## 二、05–07 的 D 片验证设计（额度、状态、证据接法）

| 验收项 | 依据 | D 的具体断言 |
|---|---|---|
| 用量未知不写 0 | 需求覆盖表 Q11/Q13 | 报告与排行榜聚合里任一组分缺值 → 该值为 `null` 并给覆盖数；`cost_usd` 只填真实美元 |
| 平台不自动补满 | Q11 与任务 05 第 6 项 | 401/429/5xx、断流、超时、未知 usage、超限都不自动重试、不切换模型、不静默增额；失败保留在案，不复跑筛选（provider 层 `request_max_retries` / `stream_max_retries` 置零归 E，D 只验平台侧不补满） |
| 计量状态遗失 | 任务 05 第 7 项 | 拒绝再用旧额度、不重置为满额；崩溃后只按已持久化证据收束 |
| 中断与错误分类 | Q11 + 任务 05 第 6 项 | 终态与 `failure_code` 正确分类，基础设施失败不写成"题目失败"，也不伪造 `resolved=false` |
| 取消/超时/崩溃收束 | Q14 | 协作取消只停止后续 Run；重试创建新 Job 并重新批准；现有 `tests/jobs/recovery/` 16 个用例在真实 provider 下复验 |
| 证据与秘密边界 | Q13 | 每 Run 的 patch/报告/轨迹引用可读、不含 Key、对象键或私有路径；账额观察不进入 HTTP、数据库与制品 |

## 三、现在能预演 vs 必须等真实数据

| 项 | 现在 | 必须等 |
|---|---|---|
| 12 Run 矩阵形状、每组合一次尝试、零重试 | ✅ 已预演（`test_matrix_rehearsal_twelve_runs.py`，真实 PG） | 真实任务身份与真实 provider |
| 五档/缺失语义、跨仓库同名不合并、报告聚合 | ✅ 已测 | 真实结果分布 |
| 恢复/重试/取消收束 | ✅ 16 个用例已过 | 真实中断场景 |
| 逐 Run 用量、费用、账额观察 | ⬜ 结构就位（缺值 null） | 06/07 真实调用 |
| 清理证据、代理与 Fork 回归 | ⬜ | E 的 05 链与组长机器窗口 |

## 自验证方式与结果

本文是设计文档，验证限于"引用真实、不越权、不含未验证断言"：

- 引用核对：plan 第 10 节的 12 Run/14 次/预算口径、需求覆盖表 Q11/Q13/Q14、任务 05 第 6/7 项、`tests/jobs/recovery/` 用例数（16，实数为各文件 3/3/3/3/4）均已逐项打开核对。
- 不复制他人权威：预算数字只在研究文档维护，本文只引用；B/E 的验证项不由 D 代跑或代记。
- 未执行项如实标注：所有"必须等真实数据"的行都没有写成通过。
- 无代码改动：本行动只新增本文档并修正 `job-control/ARCHITECTURE.md` 的一处过期陈述（"五道新题资格入库仍属于未完成任务 04" → 已由 C 于 2026-09-21 完成，并指到十二 Run 演练证据）。
