# M1 独立任务单发布

## 状态与情况说明

- 状态：Completed（2026-09-11）。已发布确认的任务单并核对依赖与验收覆盖；本地文档检查点仅含规格、14 份任务单及两份对应行动记录，不包含身份业务实现。提交标识与是否成功由交接第 5 节记录。
- 用户已采用 14 个纵向任务及其依赖，批准仅限 `.scratch/m1-platform/issues/` 的 14 个 Markdown 文件例外。整体拆分、默认标签及测试入口不再重复询问。
- 本次新建行动记录，不复用已完成的[规格行动](2026-09-11-m1-platform-spec.md)或旧 M0 记录。后续任务的实现分别另开记录。
- 交付范围依据[总架构第 3.1 节](../architecture/ARCHITECTURE.md#31-m1-交付边界2026-09-09-已确认)，验收目标来自[M1 规格](../../.scratch/m1-platform/spec.md)。规格保留发布当时的状态说明；当前任务进度以各任务验收项和交接入口为准，不修改父规格。
- 当前只有 M0 业务源码；M1 未实现。身份存储/接口、执行生命周期及排行统计口径等实施前检查点仍保留，确认任务顺序不等于这些设计已定案。
- 已有脏工作区包括此前架构/接口/运维同步和用户阅读材料；保留全部内容，不整批暂存。真实模型、Docker/网络探针、凭据、数据库迁移、机器设置及推送不在本行动范围内。

## 实施措施

1. 按 `to-tickets` 一任务一文件发布 01–14：端到端行为、明确依赖、默认标签和未勾选的验收项；不把具体实现路径或代码片段写进任务。
2. 为每个任务关联规格用户故事，覆盖全部 52 条；所有任务均落实独立行动记录和与风险相称的验证。真实平台和双机验收显式保留单独授权。
3. 更新交接中的“任务拆分待确认”状态，指向正式任务；在已完成规格行动中补充后续状态指针，保留当时过程。
4. 检查编号、依赖无环、故事覆盖、权限和安全边界、Markdown 链接及修改范围；核对通过后对规格与任务发布材料建立限定范围本地提交，不包含此前混合的文档增量，不推送。
5. 已完成任务 01 的只读承载检查，用户已确认最小账号方案，详见该任务 Comments；之后新开身份行动记录实施，不将身份代码混入本次发布行动。

## 实际修改的文件树

```text
E:/9.1agent_exam/                         # 既有项目根，不改源码
├─ .scratch/m1-platform/                 # 已批准的单一 M1 规格/任务范围
│  ├─ spec.md                            # 既有父规格，本次不修改；纳入文档检查点
│  └─ issues/                            # 新增：已批准的 14 文件文档例外
│     ├─ 01-owner-login-recovery.md      # 唯一所有者登录、本机引导与恢复
│     ├─ 02-collaborator-invitations.md  # 受邀协作者与成员管理
│     ├─ 03-task-agent-catalog.md        # 固定任务和 Codex 配置选择
│     ├─ 04-job-submission.md            # 冻结评测矩阵并等待批准
│     ├─ 05-owner-approval.md            # 所有者决定与排队资格
│     ├─ 06-single-run-report.md         # 单题 Worker 闭环与最小报告
│     ├─ 07-batch-progress.md            # 多题阶段、矩阵与部分失败
│     ├─ 08-safe-evidence.md             # 安全证据与轨迹查询
│     ├─ 09-job-cancellation.md          # 取消请求和实际收束
│     ├─ 10-interruption-recovery.md     # 中断收束与新建重试
│     ├─ 11-base-leaderboard.md          # 确定性基础排行
│     ├─ 12-artifact-retention.md        # 制品限额、到期维护与删除审计
│     ├─ 13-local-real-acceptance.md     # 单独授权的真实平台验收
│     └─ 14-private-remote-acceptance.md # 单独授权的私有双机验收
├─ HANDOFF.md                            # 修改：当前任务入口、推进顺序和待决设计
└─ docs/actions/                         # 既有一行动一记录目录
   ├─ 2026-09-11-m1-platform-spec.md      # 修改：后续确认指针，不改历史经过
   └─ 2026-09-11-m1-ticket-publication.md # 新增：本次发布与实际验证
```

14 文件例外理由：同一功能下保留连续编号和直接依赖引用，避免另设跨目录任务规则。风险限于短 Markdown 条目增多；已评估拆分目录会割裂同一规格，用户已批准。源代码行数、目录文件数限制不放宽。本次无程序设计模式变化；任务继续复用既有 Execution Backend/Patch Evaluator 的 Adapter 模式，不引入平行执行链。

## 修改后自验证方式

- PowerShell 读取全部任务：确认 01–14 连续编号、标题/标签/行为/依赖齐全，验收项全部未勾选；依赖与批准清单一致、不存在环，初始可开始任务仅 01。
- 汇总任务中的用户故事编号，检查 1–52 均覆盖且无越界；人工逐项核对规格的确定性判卷、权限、取消、中断、限额、输出保护、延期范围和最终验收边界。
- 检查本次相关 Markdown 的本地链接、锚点、围栏与行末空格，执行 `git diff --check`；父规格哈希保持不变。
- 与行动开始时 130 个已跟踪文件的 SHA-256 基线比较，只允许交接发生计划内新变化；其他历史增量保持原状，新文件与文件树一致。
- 提交前逐项核对暂存文件和 `git diff --cached --check`；提交后核对日志及工作区残留。文档检查不替代业务测试，不把未运行或跳过的验收写成通过。

## 自验证情况

- 14 份任务全部发布；编号 01–14 连续、默认标签正确、119 项验收均未勾选，逐项声明已确认依赖。依赖全部指向更早任务、无环；初始依赖前沿只有任务 01。
- 规格故事 1–52 全覆盖，无越界编号。人工对照任务的端到端行为及验收，保留批准不执行、单机单重型 Job、独立判卷、部分错误、取消与中断语义、证据保护/限额、内部测试隔离和 Judge/Review 延期；13/14 明确保留单独授权。
- PowerShell 文档检查通过：18 份文档，103 个本地链接、13 个锚点可解析，围栏与行末空格无错误；`git diff --check` 通过。Git 的 CRLF 和个人 ignore 权限提示未导致检查失败。
- 首次检查器在 JavaScript 编排层因误将 PowerShell 变量当作模板插值而报 ReferenceError，尚未执行文件检查；改为字符串拼接后重跑通过。这是检查方法错误，不是任务文件失败。
- 行动前后 130 个已跟踪文件的 SHA-256 对照，仅计划内 HANDOFF 发生新变化，其余 129 个保持原内容；父规格未编辑，当前 SHA-256 为 `b1a11542b8c6603ab4431acff37b74b00d2367cb864616ff03ac4e97bedc4822`。新增/更新文件与上述文件树一致。
- 本地检查点限定为父规格、14 份任务及规格/发布两份行动记录，共 17 个 Markdown 文件；不纳入混合旧增量的 HANDOFF、架构、接口、运维文档或无关目录。这些已保留的权威文档工作区增量仍是当前设计依据，单靠该提交不代表全部上下文已入 Git。
- 未修改业务源码、未运行业务测试、真实模型、Docker/网络探针、凭据实验或迁移；未推送。任务发布与设计批准不计为 M1 已实现或业务验收通过。
