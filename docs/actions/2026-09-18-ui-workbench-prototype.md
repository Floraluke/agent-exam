# 任务 01：可点击 Web 工作台原型

> 状态：Completed。首轮 A/B/C 离线 UI 原型已交付并通过检查；用户明确选择 A「侧栏工作台」作为产品基线。任务 01 已收口；不进入产品代码，不连接真实服务。

## 状态与情况说明

- 来源请求：用户在 `fengyy-fixweb` 分支明确要求“开始出原型”。
- 当前基准：分支 `fengyy-fixweb`，HEAD `625d02b`；工作区原有两个未跟踪 `%USERPROFILE%` 缓存目录，本任务不读取、不修改、不清理。
- 权威范围：以 [扩展规格](../../.scratch/ui-catalog-providers/spec.md)、[执行计划 01](../../.scratch/ui-catalog-providers/plan.md#3-01先把界面做成可点的-html)、[实现地图](../../.scratch/ui-catalog-providers/implementation-map.md#31-01-原型不进入产品构建)和[验证规范](../../.scratch/ui-catalog-providers/verification.md)为准。
- 已确认决定：提供三种结构明显不同的浅色布局；覆盖 owner/协作者、三步提交、审批、报告入口及约定异常场景；桌面优先、手机完整可用。用户最终选择纯 A「侧栏工作台」，不混入 B/C 的结构。
- 后续硬门槛：任务 02 在改产品代码前必须建立逐控件契约清单；每个按钮、链接、表单提交、筛选与分页动作都要映射到前端处理、HTTP/后端契约、权限、错误和成功后状态，纯前端动作明确标为“无后端请求”。不存在的接口不得为迁就页面而臆造。
- 明确排除：不修改 `apps/web` 产品代码，不访问 HTTP/API/数据库，不运行模型或容器，不写真实账号、凭据、网络地址或业务数据，不开始任务 02。

## 实施措施

1. 发布扩展任务 01 的本地任务单，并持续同步“原型制作中”到“首轮已交付、待用户选型”的实际状态。
2. 在 Git 忽略的专属运行目录创建离线 HTML/CSS/JS 原型，以 `?variant=A|B|C` 和可见底部切换器比较三种结构。
3. 使用明显标注的合成数据；实现角色、场景、导航、三步向导、审批、矩阵钻取和结果详情等仅内存交互。
4. 覆盖计划列出的正常、空、待批准、排队、执行、未通过、基础设施故障、取消、无权限、会话过期、断网、制品过期和证据缺失状态。
5. 进行静态结构检查、脚本语法检查、离线网络调用扫描，以及 1440/390/360 宽度的浏览器人工检查；记录实际结果和限制。
6. 同步用户最终选择 A、任务 01 完成事实及任务 02 的逐控件契约门槛；任务 02 仍需用户另行安排，不自动进入产品实现。

完成标准：三种布局可切换且 URL 可分享；两角色主要流程可点击；提交向导切换步骤不丢选择；矩阵可进入单次详情；手机主导航和主操作不被截断；所有数据均明确为演示数据且无真实网络写入。

## 受影响文件树

```text
docs/actions/
└─ 2026-09-18-ui-workbench-prototype.md          # 本行动：范围、实际变化、验证和停点
docs/architecture/modules/
└─ TEAM_WORK_ALLOCATION.md                       # 同步任务 01 完成与任务 02 契约门槛
.scratch/ui-catalog-providers/
├─ spec.md                                      # 固化 A 版选型与逐控件可追溯要求
├─ plan.md                                      # 同步任务 01 完成和任务 02 门槛
├─ implementation-map.md                       # 定义前端动作到 HTTP/后端的映射格式
└─ issues/
   └─ 01-clickable-html-prototype.md             # 本地任务单与验收项
runtime/prototype/ui-workbench-20260918-01/      # Git 忽略的抛弃式离线原型，不进入产品构建
├─ index.html                                    # 单页入口与原型提示
├─ styles/
│  ├─ base.css                                   # 设计变量、基础排版和可访问焦点
│  ├─ components.css                             # 卡片、状态、表单、矩阵和控制器
│  ├─ layouts.css                                # 三种互不相同的布局结构
│  └─ responsive.css                             # 390/360 手机与较窄桌面适配
├─ evidence/                                     # A/B/C 与手机截图，仅含合成数据
│  ├─ README.md                                  # 截图范围与非产品声明
│  ├─ browser-check.cjs                          # 本机 Chrome 的只读交互与响应式核对
│  └─ variant-*.png                              # 三套桌面与代表性手机截图
└─ scripts/
   ├─ fixtures.js                                # 明确标记的合成角色、Job、Run 与目录数据
   ├─ state.js                                   # 纯内存原型状态及动作
   ├─ app.js                                     # URL、事件委托、键盘切换和总渲染
   └─ views/
      ├─ components.js                           # 状态、导航、原型控制器与共用小组件
      ├─ pages.js                                # 工作台、列表、目录和管理页面内容
      ├─ flows.js                                # 三步向导、Job、矩阵和 Run 详情交互
      ├─ sidebar.js                              # A：侧栏作战台布局策略
      ├─ inbox.js                                # B：紧凑任务收件箱布局策略
      └─ overview.js                             # C：分区概览布局策略
HANDOFF.md                                       # 同步 01 完成及 02 尚未授权的停点
```

原型通过简单的“布局策略”选择 A/B/C 渲染函数；它只是抛弃式展示结构，不成为生产设计模式或新业务 Module。每个 JavaScript 文件控制在 200 行内，每层目录不超过 8 个文件。

## 自验证方式

- `git diff --check`：已跟踪文档无空白错误。
- `node --check <每个原型 JS 文件>`：全部脚本语法正确。
- 文本扫描：不存在 `fetch`、`XMLHttpRequest`、WebSocket、真实凭据或私有地址；所有数据带“演示/合成”标记。
- 浏览器检查：使用自启自停的本地静态服务打开 A/B/C，并直接打开 `file:` 入口，验证 URL 切换、左右方向键、角色切换、场景切换、导航、三步向导选择保持、中断收束、审批、报告矩阵钻取和返回。
- 响应式检查：1440×900、390×844、360×800；主导航、主按钮和矩阵横向滚动区域可操作，无页面级水平溢出。
- 文件指标：JavaScript 每文件不超过 200 行，各层目录直接文件数不超过 8。

## 自验证结果

- 中间浏览器检查第一次失败：协作者提交后的演示提示层在 390px 宽度覆盖顶部角色切换按钮，Playwright 等待 30 秒后如实超时。问题定位为提示层拦截指针事件，不是业务状态或浏览器基础设施故障；已将提示正文改为不拦截下层操作、仅关闭按钮保留点击能力，随后同命令复验通过。
- 第二次浏览器检查已证明提示层修复有效，随后因定位器优先匹配到场景下拉框内隐藏的“已排队”选项而超时；页面可见状态徽标实际存在。已把检查收紧到 `.status` 可见徽标，此次属于检查方法失败，不是原型状态失败。
- 首次组合静态检查在 PowerShell 解析阶段因把命令与 `$LASTEXITCODE` 放入同一括号表达式而失败，检查内容均未执行；这是检查脚本语法错误，不是被检查文件失败。已拆成独立语句后重跑。
- 收紧后的组合检查先通过 JS 语法/行数、目录文件数和零网络调用，随后秘密扫描的 `sk-` 宽泛模式把 `task-15184` 等任务 ID 误报为 Key。任务 ID 是既定公开演示标识，故不改数据，改用足够长度或 `sk-proj-` 的密钥形态复验。
- 加入中断收束按钮后的首次最终复验遇到 `ERR_CONNECTION_REFUSED`：此前临时预览服务已按收尾要求停止，而检查脚本仍假定 8093 正在监听。已让检查脚本自行创建并关闭仅绑定 `127.0.0.1` 的静态服务，避免依赖人工残留进程；这属于检查基础设施失败，不是原型页面失败。
- 静态检查通过：10 个 JavaScript/CJS 文件均通过 `node --check` 且每个不超过 200 行；原型根、`styles`、`scripts`、`views`、`evidence` 每层直接文件数分别为 1/4/3/6/6，均不超过 8。
- 零网络与秘密扫描通过：产品原型 HTML/JS 不含 `fetch`、XHR、WebSocket 或 HTTP(S) 地址，不含常见真实 Key、密码或私有 Tailscale 地址形态；所有人物、Job、题目摘要与状态均标为合成演示。
- 浏览器检查最终通过：本机 Chrome 在 A/B/C 三种布局和 1440/390/360 三档宽度均无页面级水平溢出；协作者提交→所有者中断收束/批准→报告矩阵→单 Run→缺失证据链可点击，手机提交按钮未截断，矩阵在命名区域内部横向滚动，HTTP 预览与直接双击 `index.html` 时左右方向键都能更新 URL，页面脚本错误与外部请求数均为 0。
- 视觉检查完成：生成 A/B/C 三张 1440px 桌面截图及 A 的 390px 手机截图并逐张查看；三种方案的信息层级与布局结构可区分。Playwright 首次因本地 bundled Chromium 缺失、随后普通沙箱启动 Chrome 报 `EPERM`；获准改用已安装 Chrome 后成功，未下载浏览器。
- 收口后的首次本地 Markdown 链接检查把 `C:/...` 绝对路径错误地拼到当前文档目录，因此误报 HANDOFF 中两个既有 Codex 配置链接不存在；修正检查器对 Windows 绝对路径的识别后，七份本轮关联文档的本地链接全部通过。
- 文档/边界检查通过：任务收口后重新运行 `git diff --check`、两份新增 Markdown 尾随空白检查、过期选型文字扫描和任务单未勾选项检查，均无错误；`apps_web_changed=0`、`task_01_unchecked=0`。原型目录仍受 `.gitignore` 管理，本地 8093 临时预览服务已停止，监听数为 0；未提交或推送。本轮检查时另见与本行动无关的未跟踪 `docs/actions/2026-09-18-unified-local-env.md`，已保留且未读取、未修改。
- 用户验收已完成：用户明确选择纯 A「侧栏工作台」，不组合 B/C；任务单最后一项已勾选，行动状态转为 Completed。
- 契约要求已固化：任务 02 必须先交付逐控件契约清单，纯前端动作显式写“无后端请求”，任何接口缺口先报告并取得确认；本轮未修改 `apps/web`，任务 02 未开始。
