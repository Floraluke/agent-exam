# 本地 Spec 工作流配置

## 状态与情况说明

- 状态：Completed（2026-09-11）；用户确认的配置草稿已落盘并完成文档验证。本次是同一配置行动，不因跨日另开记录。
- 本次行动：为 `to-spec`、`to-tickets` 和 `implement` 配置仓库内的工作约定；不包含 M1 业务实现。后续规格、任务拆分和每次实施行动分别新建记录，同次行动的前后验证在原记录内更新。
- 来源：用户要求先做好行动文档，再用 Spec 分任务、逐步开发 M1，并在关键节点本地提交；2026-09-10 确认采用本地 Markdown。
- 行动开始时已查明：存在根目录 `AGENTS.md`、`CONTEXT.md` 及 `docs/adr/`；不存在 `CLAUDE.md`、`docs/agents/` 或 `.scratch/`；本机已安装相关工程技能和 `triage`。现有忽略规则未排除本地任务目录。
- 确认的设计：新增 `docs/agents/` 专门承载技能消费约定，新增 `.scratch/` 承载规格和独立任务单；现有 `docs/actions/` 继续负责行动过程及验证证据，不能替代前两者。
- M1 业务范围仍以[总架构](../architecture/ARCHITECTURE.md)为准；本次不重新定义范围，也不调整业务模块或接口。
- 保留已有未提交文档及无关 `%USERPROFILE%/` 目录；不批量暂存。真实模型、Docker/网络探针、凭据使用、机器设置和推送不在本次范围内。

## 实施措施

1. 已完整阅读配置、行动记录和面向 Agent 写作技能，检查已有配置与工作树。
2. 已确认默认任务标签及完整草稿，原样写入三个配置文档，并在 `AGENTS.md` 新增唯一入口。
3. 已采用单一领域上下文，复用现有术语表与 ADR；未复制业务定义，也未新增领域模型。
4. 已同步交接状态及总架构的配置导航、目录职责；`.scratch/` 留待首次发布规格时创建。
5. 文档验证通过，本次独立配置检查点限定 `AGENTS.md`、三个配置文档及本记录；已混合前轮增量的总架构及交接保留在工作树，不整文件混入本次提交。提交结果以本地 Git 为准，不推送。

完成标准：配置约定经确认、三个配置文档与唯一入口一致、文档检查通过、能够继续执行规格技能。配置成功不等于规格已发布或 M1 已实现。

## 实际修改的文件树

```text
E:/9.1agent_exam/
├─ AGENTS.md                         # 已修改：增加工程技能的简短入口
├─ HANDOFF.md                        # 已修改：记录配置完成及后续测试边界确认
└─ docs/                             # 现有项目文档目录
   ├─ actions/                      # 现有行动记录目录
   │  └─ 2026-09-10-local-spec-workflow-setup.md  # 新增：本次配置的措施与实际验证
   ├─ agents/                       # 已新增：工程技能约定的唯一维护位置
   │  ├─ issue-tracker.md            # 本地规格、任务单的位置与读写约定
   │  ├─ triage-labels.md            # 默认角色与实际标签的映射
   │  └─ domain.md                   # 复用现有术语表与架构决定的阅读规则
   └─ architecture/                 # 现有架构文档目录
      └─ ARCHITECTURE.md             # 已修改：补充已确认目录职责及配置导航
```

本次无新增程序设计模式；协作关系为 `AGENTS.md` → 配置约定 → 本地规格/任务文件。行动记录引用这些约定，不另行维护同一规则。本次未创建 `.scratch/`，未创建正式规格或任务单。

## 修改后自验证方式

- 用 PowerShell 检查实际修改文档的相对链接、标题锚点、代码围栏和行末空格；所有引用须可解析。
- 执行 `git diff --check`；成功标准为无差异格式错误。
- 检查 `AGENTS.md` 的 `Agent skills` 入口仅有一处，三个配置文档的职责与链接一致。
- 用修改前的 SHA-256 基线核对全部已跟踪文件；只允许文件树中的计划变更，不修改业务代码或已有无关文档。
- 本次为文档配置，不运行 pytest、模型、Docker 或外部任务发布；不得把未运行的测试报告为通过。

## 自验证情况

- 已完成环境和配置存在性检查，确认本地配置尚未创建；已保存本次修改前的已跟踪文件哈希基线。
- 用户在标签含义说明后确认保留五个默认标签；已展示完整配置草稿，并在解释职责分工后于 2026-09-11 获确认，现已原样写入。
- 已执行 PowerShell 文档检查：本次行动文档和交接共 2 份，79 个本地链接及其指定锚点可解析，代码围栏和行末空格检查无错误。
- 首次检查脚本把字符替换误用为字符串删除，产生类型转换错误；已仅修正检查脚本并重跑通过。这是检查方法错误，不是项目链接或业务故障。
- `git diff --check` 通过；出现既有换行转换提示，无差异格式错误。
- SHA-256 基线核对 126 个已跟踪文件：仅计划内 `HANDOFF.md` 改变，其余 125 个保持本次行动开始时内容；新增本行动文档。未改动业务代码及已有无关文档。
- 2026-09-11 完整配置检查通过：7 份文档、122 个本地链接、19 个锚点均可解析，围栏及行末空格无错误；`git diff --check` 通过。唯一入口 1 处、配置文件 3 份、五个默认标签各 1 处，人工对照与已确认草稿一致。
- 2026-09-11 重新建立落盘前基线并核对 126 个已跟踪文件：仅 `AGENTS.md`、`HANDOFF.md`、总架构改变，其余 123 个保持内容；本次新增三个配置文档，继续更新本记录。没有改动业务代码、个人技能、真实运行证据或无关目录。
- 配置已完成；接下来使用 `to-spec`，不再询问 tracker、标签或配置草稿。规格的测试边界及任务粒度尚待相应技能流程确认，不等于 M1 已实现。
- 未运行模型、Docker、业务测试或推送；本次只验证文档和配置约定，不声称业务回归通过。
