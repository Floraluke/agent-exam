# 任务 05 交付要求（负责人侧）

> **负责人回执（2026-09-21）**：第 1 节 9 项决定已填写；资源范围已确认。本轮仅同步分支和文档，**不运行拓扑探针、不创建或删除 Docker 资源**。
>
> **用途**：这份文件可直接交给负责人执行，也可整份转交给负责人的 AI 助手。它需要负责人的**授权**和**他那台机器**。
>
> **一句话**：需要你做三件事——① 拍板 9 项数值；② 做 6 项前置核对；③ 用最小替身跑一次拓扑实证。
>
> **顺序提示**：② 不满足就都别做；③ 的结论决定代理代码怎么写，建议**先做 ③**，①的第 7 项可以与 ③ 并行想。
>
> 编写：成员 E（LLY）　日期：2026-09-21

## 0. 背景（30 秒）

任务 05 叫"假提供方安全执行链"：让**假 Key** 走完正式执行链，把秘密隔离、请求边界、额度计量与生命周期收束固定在可证的事实上，为将来放行真实 Key 做门禁。**本任务不接真实 Key、不调用真实供应商接口、不充值。**

代码大部分由 E 在开发机上写。但其中**容器与网络隔离**这部分必须在你的机器上做——E 的开发机没有 Docker，也没有固定的执行框架与镜像。所以需要你出下面三样东西，E 才能继续写下去。

## 1. 第一件：负责人 9 项决定（已回填）

前 6 项是研究文档给的**工程初值**，最后一列记录负责人于 2026-09-21 确认的决定。

| # | 待定事项 | 研究初值（建议起点） | 你的决定 |
|---|---|---|---|
| 1 | 每 Run 输入累计上限 | 300,000 Token | **300,000 Token** |
| 2 | 每 Run 输出累计上限（含推理） | 32,000 Token | **32,000 Token** |
| 3 | 每 Run 模型期限 | 900 秒 | **900 秒；与环境构建、独立判卷分开计时** |
| 4 | 起始请求频率 | 最多 3 次/分钟，并服从更低实际账户限额 | **采用；账户限额更低时从低** |
| 5 | 首轮支出目标 | 合计不超过 ¥100 | **采用；只称计划目标，不称硬上限** |
| 6 | Kimi 项目日/月预算 | 建议 ¥80（官方说明约 10 分钟延迟生效，只作第二层保护） | **日/月各 ¥80；本轮未执行账户设置** |
| 7 | **输入 Token 计数方式** | 见下方说明 | **A 保守上界；证明不低估，不承诺精确账单** |
| 8 | 私有秘密文件的宿主路径与属主 | 只有你知道，不入 Git | **已选仓库外私有路径；当前 Windows 用户为属主；绝对路径不入 Git** |
| 9 | 本片工作的机器窗口与授权范围 | 起停专属容器与网络 | **确认专属资源范围；本轮无执行窗口、不运行探针** |

### 第 7 项要单独说，因为它是唯一会导致返工的

要求原文是"必须定义并验证与供应商相符的输入计数或保守上界；**普通字符串长度不是准确 Token 数**"。两条路：

- **A. 保守上界**（推荐起步）：把字符数按一个偏高的比例折算成 Token，永远高估。实现简单、无需验证 tokenizer；代价是**账本只能承诺"不超过某个上限"，不能承诺精确账单金额**。
- **B. 精确计数**：需要与供应商一致的 tokenizer，并先验证计数结果与供应商实际计费一致。更准，但要额外验证工作量；未验证前同样不能承诺硬上限。

选哪个直接决定账本算法怎么写，也决定后面敢不敢说"硬上限"。**如果不选，E 只能按 A 做，并在文档里写明"不是精确账单"。**

## 2. 第二件：前置核对（6 项，任一不满足即停）

请逐项确认，任何一项不满足就先停下来告诉 E，不要在现场想办法绕过。

- [ ] 你的机器 Docker 可用，能创建自定义网络与临时容器。**只读确认 Engine 27.5.1 可响应；本轮未创建资源，创建能力未实证。**
- [x] 固定的执行框架源码（`framework/harbor`）在，revision 与项目依赖表一致；**不要重建它**——HEAD 为 `6af8d6e31eced13b93849cdf80feeadf24603d15`，工作树干净。
- [x] 你的机器当前**没有**正在运行的 AgentExam 业务 Job；2026-09-21 本轮复核 `activeJobs=0`。PostgreSQL / MinIO 持久化服务仍在运行，本片**不停止、不删除、不连接**它们。
- [x] 本片改动范围限于专属的项目名、网络名与标签；负责人已确认回执第 2.2 节范围，**不动共享 Docker/WSL、全局代理、防火墙或已存在的容器**。
- [ ] 本片只用**假 Key**，私有假值文件放在仓库之外、权限最小化；路径与属主规则已确认，但文件尚未创建或验权。本轮**不读取任何真实 Key、登录文件或账户凭据**。
- [ ] 你同意本片的操作范围：资源范围已确认，但本轮选择“只填文档、不运行探针”，没有当前创建/删除授权与时间窗口。

**前置总判定：STOP（3/6 完整满足）。** 9 项决定已经完成，但不得把资源范围确认解释为当前执行授权。

> **2026-09-21 修正（由负责人核对发现）**：原第 3 项措辞为"当前没有运行中的 AgentExam 业务 Job **或容器**"。负责人实际核对时指出：持久化服务容器正在运行（活动 Job 为 0），该条件**按字面不成立**。这是原文把"业务 Job"与"持久化服务"混为一谈的缺陷，已拆开表述并明确**探针必须避开既有持久化服务、不得为跑探针而停止或删除它们**。负责人填写的原始核对记录见[负责人填充版](TASK05_OWNER_DELIVERY_FILLED.md)。

## 3. 第三件：最小替身拓扑实证（建议先做这一件）

> **本轮结果**：未执行。7 条断言均无新命令、输出、网络图、镜像 digest 或清理证据；以下内容仍是后续获单独授权时的执行要求。

### 3.1 要证什么

只有一句话：**做题侧容器除了能到专属代理以外，没有任何其他通路，而受控出网只存在于代理侧。**

权威要求是三组对照，缺一不可：**直连拒绝、宿主隔离、正常模型请求正对照**。

### 3.2 为什么先做

因为这里有一个**未决的技术问题**：项目现有的网络侧车是"Harbor 主容器 + 侧车共享网络命名空间"的结构（其 compose 配置刻意不声明 networks，由执行框架自己附加侧车）。这个结构**不能**承载"只通代理"的边界。所以本片第一个要回答的就是：**这套固定框架是否允许替换或绕过它的网络附加方式。** 这一点只有在你的机器上才能试出来。

如果不可行，任务 05 按停止条件停在这里，不会浪费 E 写完整代理的工时。

### 3.3 用什么做

用**一次性探针**，不是产品代码。探针放在被 Git 忽略的证据目录里（例如 `runtime/prototype/topology-proof-<日期>-<序号>/`），和项目以往所有探针的做法一致（探针脚本 + 配置 + 输出摘要 `summary.json`，保留供复核）。探针**不进入产品源码树**。

做题侧与代理可以先用**最小替身**：一个只做"转发 + 拒绝判定"的脚本就够，不需要实现完整的代理逻辑。

### 3.4 步骤（顺序固定）

1. 起两组网络：内部网络 A 只含"做题侧 + 代理"；代理侧有受控出网。做题侧**不接**出网网络，也不接默认网络。
2. 逐条跑下面的断言，**每条都要留下实际命令与实际输出**。
3. 做一组正常请求的正对照（见下方第 6 条）。
4. 记录网络图、容器/网络/镜像的固定身份。
5. 精确清理：只删除本任务标签下的容器、网络与卷，复核残留为 0；**不执行全局 prune**。

### 3.5 断言清单

| # | 断言 | 期望 | 性质 |
|---|---|---|---|
| 1 | 做题侧 → 代理固定入口 | **通** | 正 |
| 2 | 做题侧 → 任一公网目标 | **不通** | 反 |
| 3 | 做题侧 → 宿主网关 / 云 metadata 地址 / 其他 Trial 所在网络 | **不通** | 反 |
| 4 | 代理 → 固定假上游 | **通** | 正 |
| 5 | 无主机发布端口、无 Docker 套接字挂载、无可写宿主挂载（若设计上必须保留最小挂载，记录其范围与理由） | 符合 | 结构 |
| 6 | 用**假 Key**走一次"做题侧 → 代理 → 假上游"，确认请求确实经代理发出并被记录 | 通 | 正对照 |
| 7 | 做题侧看不到代理进程与代理私有的假值文件 | 看不到 | 隔离 |

## 4. 不要做

- **不读取真实 Key、登录文件或账户凭据；不发起真实供应商请求；不充值。**
- 不放宽到公网。
- 不把真实 Key 放进做题容器（这个方案已被明确放弃）。
- **不照搬现有网络侧车**——它与主容器共享网络命名空间，照搬后不能宣称防绕过。
- 不改共享 Docker/WSL、全局代理、防火墙或既有容器。
- 不下载大体积镜像（本片使用已有缓存的镜像）。
- **不为让断言通过而修改断言或跳过条目**；跳过项必须如实列出。

## 5. 回报什么、回到哪里

把以下内容交回给 E（不要只给截图或口头结论）：

1. 网络图（哪几个网络、谁接谁、出网在哪一侧）。
2. 上面 7 条断言各自的**实际命令与实际输出**。
3. 容器 / 网络 / 镜像的固定身份（名称、标签、镜像 digest）。
4. 清理复核结果（残留是否为 0）。
5. 失败项、偏差与未验证项——**如实列出，不修饰**。

这些会写进任务 05 的行动文档，作为这一片的证据。

## 6. E 侧同时在做什么（关键路径）

- **数值阻塞已解除**：第 1–7 项已确认，其中账本采用 A 保守上界。
- **仍等正式开工授权**：请求白名单、权限校验、配置渲染、假服务、请求计数器与账本仍未因本回执自动获准实施。
- **仍等机器窗口**：代理网络部分、7 条拓扑断言与集成层验证没有执行授权。

另外有一条已核实的事实会影响设计，提前告知：固定 Codex 的官方配置里 `request_max_retries` 与 `stream_max_retries` **默认值不是零**，而且命令行可以覆盖模型名——所以"把配置文件写对"不足以证明没有重发，必须由代理侧独立验证。这一条已写进设计底稿。

## 7. 权威对应位置（冲突时以下列文件为准）

| 内容 | 权威位置 |
|---|---|
| 代理机制、秘密边界、强制安全约束 | `docs/interfaces/CODEX_AUTHENTICATION.md` 第 4.1、5 节 |
| 负例矩阵与验收要求 | `.scratch/ui-catalog-providers/verification.md` 第 4 节 |
| 型号、端点、价格、额度初值、实现约束 | `docs/research/2026-09-17-codex-provider-config-and-budget.md` 第 2–5 节 |
| 05 的步骤、验收与停止条件 | `.scratch/ui-catalog-providers/plan.md` 第 7 节 |
| E 侧设计底稿（9 项拍板的出处） | `docs/LLY/01-plan/STAGE1_PROXY_DESIGN_FREEZE.md` |

本文已记录负责人决定，但**不替代**上述权威文件，也不构成任务 05 的实施开工或拓扑执行授权。
