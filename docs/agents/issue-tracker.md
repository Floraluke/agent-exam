# 本地任务约定

- 每个功能使用 `.scratch/<feature-slug>/` 目录。
- 规格保存为该目录下的 `spec.md`。
- 每个任务单独保存为 `issues/<NN>-<slug>.md`，编号从 01 开始。
- 文件顶部使用 `Status:` 记录标签，取值见 [标签映射](triage-labels.md)。
- 讨论补充到文件末尾的 `## Comments`。
- 技能要求“发布”时创建本地文件；要求“获取”时读取对应文件。
- 完成情况以验收项和行动记录为证，标签不能替代验证。
