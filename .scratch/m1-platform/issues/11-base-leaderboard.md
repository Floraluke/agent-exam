# 11: 基础排行榜

**What to build:** 用户通过页面和 HTTP 查看按完整 Agent 配置区分的确定性基础排行榜，理解比较条件、未知结果与基础设施错误；不引入模型主观评分或虚假的破同分规则。

**Blocked by:** 07 多题进度与部分失败

**Status:** ready-for-agent

**Spec stories:** 45, 46, 47, 51

- [x] 实施前提出并由用户确认重复 Job/手动重试结果选择、统计分母及可比范围的最小业务口径；不从“通过率”一词自行猜测规则。
- [x] 按 Agent、版本、模型提供方、模型和关键配置的完整身份分组，页面明确显示比较条件，不混合不可比任务范围或限制。
- [x] 分数仅来自可信确定性结果；题目未解决、未知及基础设施错误分别处理并展示，统计结果按已确认口径可追溯到纳入的 Job/Run。
- [x] 过程指标只展示，缺失为 null/unknown 而不是 0；确定性同等成绩保持并列，不用质量评分或任意顺序制造胜负。
- [x] 内部测试/替身数据不能进入正式排行榜、正式报告或其聚合来源；测试可在隔离测试范围验证聚合，不污染正式展示。
- [x] HTTP 和页面的过滤、分页、空结果、错误状态符合既有契约；延期 Judge/Review 保持兼容空值，不创建对应表、路由或依赖。
- [x] 通过固定合成数据验证聚合分母、重复尝试、并列与排除；真实 PostgreSQL 集成验证查询口径，少量浏览器测试验证展示与来源跳转。
- [x] 独立行动记录记录业务决定及其权威文档位置、实际验证与遗留项；关键节点本地提交，不执行真实模型或推送。

## Comments

2026-09-11：任务已发布，尚未实施。任务顺序已确认，但尚未定义的统计口径没有因此自动定案。

2026-09-13：任务 11 验收完成。用户确认的最早可信确定性结果、重试只填空、全题分母、完整冻结范围/Agent 身份、已开始正式尝试门槛、并列及过程指标口径均已实现；页面显示完整条件并可追溯 Job/Run。生产 SQL 先排除 `internal_test`，再联结目录 Task/源 Artifact/Agent 配置对正式冻结证据和 Harbor 身份失败关闭。最终专属 PostgreSQL+MinIO 为 `115 passed`，完整后端为 `359 passed, 76 skipped`，完整浏览器为 `20 passed`，Ruff、mypy、Web typecheck/build 与规模检查通过。以 `6ccbd18` 为固定基准的 Standards 与 Spec 终审均 PASS；实现及评审修复提交为 `83c29d8`、`e802aa4`。未运行真实模型、Harbor 或 Judge，未推送。
