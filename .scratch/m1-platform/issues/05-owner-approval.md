# 05: 所有者批准或拒绝

**What to build:** 所有者从 Job 详情核对冻结摘要并批准或拒绝，提交者看到真实决定和状态；批准只给予排队资格，拒绝则终止未开始的运行。

**Blocked by:** 04 提交并冻结评测 Job

**Status:** ready-for-agent

**Spec stories:** 18, 19, 20, 21, 51

- [x] 所有者页面展示足以判断运行范围的冻结摘要，并通过 HTTP 批准或拒绝；提交者刷新可见决定结果。
- [x] 只有可信 owner 会话可决定；协作者即使提交自己的 Job 也不能批准，正文不能指定决定者身份或覆盖冻结内容。
- [x] 只允许待批准 Job 转移；批准成为 QUEUED，不在 HTTP 请求内读取凭据、启动 Harbor 或执行判卷。
- [x] 拒绝使 Job 终止且未开始的 Run 取消；队列查询不包含待批准和已拒绝 Job，随后 Worker 接线继续验证实际不可领取。
- [x] 决定者、时间、说明和状态事件原子保存；重复/并发批准与拒绝只形成一个一致决定，冲突符合现有版本和 HTTP 契约。
- [x] 不可用数据库或事务失败不产生假成功；页面显示拒绝、权限和并发冲突，不将旧状态当作已批准。
- [x] HTTP 覆盖所有者决定、越权和不执行不变量；浏览器覆盖批准/拒绝，PostgreSQL 集成覆盖并发决定、队列资格和回滚。
- [x] 新建独立行动记录并同步状态/接口文档，验证后作限定范围本地提交；本任务不开展真实模型或容器实验，不推送。

## Comments

2026-09-12 完成：实现提交 `d3148e1`，评审修复 `94ad4b6`、`2dfd427`；owner 可核对完整冻结摘要后批准为 `QUEUED` 或拒绝为 `REJECTED` 并原子取消 `PENDING` Run，可信身份、幂等、并发、回滚和安全错误均已验证。最终后端 `289 passed / 56 skipped / 2 warnings`，专属隔离 PostgreSQL `24 passed / 2 warnings`，Web typecheck/build 通过，五组浏览器累计 14 passed；Standards/Spec 原评审者最终复核均通过。56 个门禁跳过项不冒称通过，本任务没有启动真实模型、Harbor/判卷或长期服务，也未推送；任务 06 未开始。完整失败史、容器精确清理和文件指标见[行动记录](../../../docs/actions/2026-09-12-m1-owner-approval.md)。

2026-09-12 用户确认决定说明策略：说明可选；填写时去除首尾空白、限制 1–500 个 Unicode 字符并拒绝控制字符；页面提示不得填写凭据、Token 或宿主路径，不做误判风险高的秘密正则扫描。needs-info 已解除，沿同一行动开始 HTTP 红绿实现，其余已确认范围不重复询问。

2026-09-12 开工核对：用户已授权开始任务 05；已建立[独立行动](../../../docs/actions/2026-09-12-m1-owner-approval.md)，计划只深化已规划 Owner Approval、既有 JobRepository 与四张既有表，不新增表或顶层目录。HTTP/浏览器/真实 PG seam 已由 M1 规格确认。开工时自由文本策略曾是唯一 needs-info，随后已由用户明确裁决并在上方记录，不再构成阻塞。

2026-09-11：任务已发布，尚未实施。Owner Approval 是运行前授权，不属于已延期的 Judge 结论 Human Review。
