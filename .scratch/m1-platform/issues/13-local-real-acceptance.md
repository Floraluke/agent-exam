# 13: 本机真实平台验收

**What to build:** 在用户单独批准的真实运行范围内，通过新平台完成提交、所有者批准、排队、真实 Codex 执行、固定 Fork 判卷、持久化报告与安全证据查询，形成可追溯的本机验收结论。

**Blocked by:** 09 取消流程；10 中断恢复与新建重试；11 基础排行榜；12 制品限额与到期清理

**Status:** in-progress

**Spec stories:** 47, 51, 52

- [x] 运行前逐项核对 Harbor/认证专题剩余安全、网络与生命周期门槛，区分历史通过、当前缺证、明确阶段例外和真实阻塞；不因 M1 开发而自动豁免。
- [x] 对真实模型、凭据、容器/网络验证的目的、固定输入、额度、风险、退出及精确清理方案取得本次单独授权；未获授权时仅可做安全只读准备。
- [ ] 新平台真实入口完成提交→等待批准→唯一所有者批准→Worker 领取→真实执行→独立判卷→PostgreSQL/MinIO 持久化→页面报告；创建/批准请求自身仍不执行模型。
- [x] 核对固定任务、完整配置、一个平台 Job/一个 Harbor Job、逐题身份和 patch 哈希；实际判卷结论和模型/基础设施失败如实保存，不以成功愿望替代证据。
- [x] 检查正式结果与内部测试数据隔离、排行来源、安全证据及兼容空值；未满足发布保护的原始内容不能经页面/下载流出。
- [x] 在已授权范围验证适用的生命周期、权限、资源与精确清理门槛；取消与恢复交叉行为、存储故障等本地软件回归完整，未获真实验证的项明确保留。
- [x] 旧 M0 第四场只作为历史基线，不能计为新平台已通过；失败不自动重跑，需要新范围或额度时重新取得授权。
- [ ] 独立行动记录保留冻结输入、实际结果、证据指针、验证限制和问题状态；敏感原始材料不入 Git，提交仅含可公开代码/文档且不推送。
- [ ] 明确本机验收的通过/未通过项；只有适用门槛全部满足才可宣称本任务完成，私有双机验收尚由任务 14 承担，不提前宣布 M1/MVP 完成。

## Comments

2026-09-11：任务已发布，尚未实施。ready-for-agent 只是任务标签，不是本次真实模型、Docker、凭据或机器变更的执行许可。

2026-09-13：正式 Worker Composition Root、目录→Harbor 固定配置映射、无模型安装/超时清理门禁、专属 PG/MinIO 135 项回归和零模型编排预检已完成；证据见 `docs/actions/2026-09-13-m1-local-real-acceptance.md`。首次真实编排命令在进程创建前被安全审批拒绝，未创建 Job、读取认证或访问模型；其余八项仍等待唯一真实 Run 及后续验证，不得自动重试或绕过。

2026-09-13：用户承接完整逐项文本明确授权后，唯一真实 Run 已执行且未重试；HTTP 提交/所有者批准成功，Harbor 形成 1302 字节补丁，但正式 Worker 组合遗漏既有本地证据 reader，平台在判卷前以 `EVIDENCE_UNAVAILABLE` 如实失败并完成精确清理。`10f0c53` 补回 reader 后，双轴评审又发现 Fork 项目相对引用的解释基准仍错误；`3d66230` 已用 Harbor 绝对引用、Fork 相对引用和逃逸拒绝行为测试完成无模型修复。授权项已完成，其余七项仍未通过；任何第二次真实 Run 需重新授权。

2026-09-13：新的 `m1-task13-20260913-02` 只复制 10 个编排源码/测试文件，除专属 scope 身份外与首次编排器零差异；未复制首次私有证据。单元/静态检查和零模型 preflight 通过，确认零 Job、零认证读取、零模型调用并清理全部专属容器/网络；这不替代第二次真实 Run 授权或剩余七项验收。

2026-09-13：用户连续回复“批准你”“批准”，明确承接第二次且最后一次 Run 的完整固定范围；可在 `m1-task13-20260913-02` 使用现有私有认证与固定模型网络执行唯一一次模型尝试，失败不重试、不使用额度重置，并在结束后精确清理。

2026-09-13：第二次且最后一次真实 Run 已执行且未重试。平台 Job/Run 均为 `COMPLETED`、固定 Fork `resolved=true`、1225 字节 patch 三处哈希一致，公开确定性计数为 submitted/resolved/error=`1/1/0`，专属资源清理通过；最终 `status=failed/phase=verification` 来自后置验收器误把 Harbor Trial 配置当成 Job 配置，并非真实执行或判卷失败。固定 Harbor 的 Job/Trial 模型确认生产源配置为一次尝试/零重试；忽略态验证器已用红绿测试改为分层核对，合成 5 项和同一真实证据离线复验均通过。浏览器步骤因验证器提前退出且临时存储随后精确清理而没有执行，任务仍保持 `in-progress`，不得把离线修复或历史页面证据冒充本次真实页面通过。

2026-09-13：用户随后明确允许使用既有私有认证最多再执行 3 次受控全流程，不需逐次等待；先修复/完成任务 13，再进入任务 14，并在任务 14 后执行全量回归。次数为上限，每次继续固定本任务既有输入/网络/资源并使用新隔离 scope，失败先诊断修复、不并发、不自动重试；认证正文不展示、不入 Git，当前额度先用至约剩 1–2% 再保存并使用唯一可用重置。双轴评审发现当前分层验证器仍漏核对 Trial 固定身份与部分安全配置，须先完成无模型红绿修复后才使用新增授权的第 1 次。

2026-09-13：身份、完整安全配置、固定任务及 GPU/TPU 负例补齐后，第二轮复审只剩“任务入口在解析后才查链接”的共同 finding；新增负例先稳定复现未拒绝，再改为对未解析入口先检查链接、严格解析并核对仍在本次 `tasks` 根内。忽略态测试 `8 passed`、静态/编译检查通过，同一第二次私有证据只读离线复验为 `PASS`；待原评审者确认 finding 关闭后，才使用追加授权的第 1 次新 scope 全流程。

2026-09-14：`-02` 验收器两轴终审已为 0 findings/PASS；新 `m1-task13-20260914-03` 排除全部旧证据后复制 14 个源码/测试文件，归一 scope 后零意外差异。首次零模型 preflight 因容器内 PostgreSQL 已就绪而 Windows 主机端口尚未可连，以 `IdentityUnavailable` 安全失败并清理；复跑通过。随后用红绿测试把就绪门禁深化为容器内 `pg_isready` 加主机同一 DSN `SELECT 1`，并以单调时钟绝对截止在 30 秒总预算内；全套 `10 passed`、静态/编译检查和修复后 preflight 通过，专属资源为空。此次无 Job、认证或模型调用，不消耗新增三次授权；待两轴确认该门禁及状态同步后开始第 1 次真实全流程。

2026-09-14：门禁复审进一步按 libpq“连接超时最少 2 秒”语义补齐 30 秒尾段：持续失败测试先让中间实现得到 `clock=30.9 > 30`，最终在剩余不足 2 秒时直接结束，避免连接调用越过绝对截止。定向 2 项、全套 10 项及静态检查通过，最终零模型 preflight 仍通过并清理；等待两轴最终清零。

2026-09-14：`-03` 门禁两轴复核清零后，使用新增授权第 1 次执行真实全流程且未重试。平台 Job/Run、固定 Fork、一个 Trial、正式排行及安全证据均完成，`resolved=true`；最终仅在浏览器阶段以 `CalledProcessError` 失败并精确清理。源码对账发现浏览器脚本仍断言旧文案“状态：全部完成”，而当前页面和既有浏览器契约对 `COMPLETED` 显示“执行完成”，这是进入后续断言前的必然阻断点；摘要未保存 Node stderr，故不冒称拿到运行时精确行号。先无模型修复/合成验证/复审，不自动使用第 2 次授权。

2026-09-14：同一 `job-batch` 合成浏览器流程在默认环境复现 Playwright 1243 可执行文件缺失，证明真实失败在页面断言前的直接阻断是验收器未绑定项目已有固定浏览器缓存；只给测试进程设置依赖文档已有的 `PLAYWRIGHT_BROWSERS_PATH` 后 `1 passed`，无下载或机器变更。验收器现于存储/Job/模型前核对并绑定固定缓存，同时把必然过期的完成态文案改为实际可访问标题；忽略态 `11 passed`、静态/语法检查和零模型 preflight 通过。最新 preflight 覆盖默认摘要后，真实运行的原样安全输出另存并明确区分；待双轴复核后才决定是否使用第 2 次。

2026-09-14：复审要求把浏览器缓存真正锚定在项目内并恢复目录指标。junction 与解析到项目外两个负例均让旧实现得到 `DID NOT RAISE`，修复后逐级拒绝 symlink/junction，且解析路径必须精确等于项目内固定 1243 可执行文件；摘要、Web 日志和未来截图移入 `results/`，根层由 11 文件降为 7。忽略态 `13 passed`、静态/语法检查和最终零模型 preflight 通过。第 4–7 项已由本次真实 verify、既有完整软件回归及三次均无自动重试的事实独立满足；页面全链、最终证据提交和任务关闭仍不勾选。

2026-09-14：`m1-task13-20260914-04` 已完成源码等价对账、13 项忽略态测试、静态/语法检查和零模型 preflight，且专属 Docker 资源清理为空。随后完整真实运行在进程创建前被安全审批器拒绝；审批器要求用户在获知风险后明确同意把固定验收题目和运行请求经 `auth.openai.com` / `chatgpt.com` 发给 `openai/gpt-5.6-terra`。本次未启动、未读 auth、未建 Job、未调用模型且不计次数；不得绕过，故第 3、8、9 项仍未完成，任务 14 也不提前开始。
