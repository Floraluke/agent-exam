# 04: 提交并冻结评测 Job

**What to build:** 协作者在页面选择任务、已登记 Codex 配置和限制模板，查看组合数量并提交；平台原子创建冻结的 Job 和逐题运行，立即展示等待所有者批准。

**Blocked by:** 02 邀请协作者与成员管理；03 任务与 Codex 配置目录

**Status:** ready-for-agent

**Spec stories:** 12, 13, 14, 15, 16, 17, 47, 51

- [x] 页面选择已登记任务、配置、闭卷赛道和限制模板，提交前展示组合数量；demo 1–3 题、quick 5 题、standard 10–20 题，每批最多 3 个启用配置、最多 60 条 Run。
- [x] 服务端拒绝非法、未登记、禁用、超限及任意命令/地址/资源覆盖请求；调用人仅来自可信会话。
- [x] 一次短事务创建 AWAITING_OWNER_APPROVAL Job 及全部组合的 Run，不出现缺少组合的半批次，失败整体回滚。
- [x] 创建时冻结任务、Agent、后端版本、赛道、网络/工具策略和限制；目录后续变化不影响已提交内容。
- [x] 同一调用范围内，同一幂等键与相同正文返回同一 Job；同键不同正文明确冲突，并发重发不重复创建。
- [x] HTTP 创建只返回等待批准状态，不读取模型凭据、不启动 Harbor、不等待评测；提交后的详情页面可刷新并从数据库恢复。
- [x] 合成数据和替身提交处于内部测试环境/范围，不进入正式数据、报告或排行榜；客户端不能借内部测试标记绕过批准。
- [x] HTTP 和浏览器验证选择→提交→等待批准；真实 PostgreSQL 集成验证事务、幂等并发及快照稳定，不仅验证内存行为。
- [x] 本任务独立行动记录保存验收证据、文档同步和遗留项；关键节点核对范围后本地提交，不推送。

## Comments

2026-09-12 最新状态：任务 04 九项验收完成，同一行动为 Completed。实现检查点 `5b585b4`；以 `3302084` 为固定基准的 Standards/Spec 评审共识别 5 个不重复问题，均经红绿测试修复并由原评审者只读复核为 resolved。最终默认后端、专属真实 PG、静态/构建、完整浏览器与文档检查均通过；跳过项、上游警告、隔离清理和未实施的任务 05 边界见本任务行动。Status 沿用本地既有标签，完成事实以验收项和行动记录为准。以下未标明“最新状态”的内容均为历史过程记录，不覆盖本段。

2026-09-12 恢复窗口：用户明确确认任务行动中的最小方案，包括精确规模/最多三个配置、`default-single-host-v1` 初始资源模板、查询访问范围、四张规划内表、必要 Interface 和子目录；原 needs-info 是未回填旧快照，现改为 ready-for-agent 并直接实施，不重复询问。任务 05 仍不在本轮范围。

2026-09-12 开工核对：任务 02、03 已完成，目录收尾检查点 3302084。新建[本任务独立行动](../../../docs/actions/2026-09-12-m1-job-submission.md)，只读核对后提出精确规模、初始资源预置、查询范围及必要四表/Interface/子目录方案；needs-info 仅表示等待这些未定选择，不重新申请整体开发、临时 PG 或本地提交授权。任务 04 业务代码、数据库和测试尚未执行，验收项全部未勾。任务 05 不在本轮连续实施范围。

2026-09-11：任务已发布，尚未实施。任务 05 完成前只能提交等待批准，不宣称平台已能执行。
