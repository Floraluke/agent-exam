Status: ready-for-agent

# 持久化与长期运行：已前移的 P 阶段

> 2026-09-17 用户已发布最小本地持久化目标，并确认 `infra/` 与验收入口；暂停后又明确“继续目标”，现按行动指南 P1–P4 推进，不恢复已取消的备份与恢复。课设范围、手动启停、数据根目录与无备份风险已确认；固定镜像和配置已核验，D 盘挂载和运行隔离仍未落实。实际部署未运行；证据见[实施行动](../../docs/actions/2026-09-17-minimal-local-persistence.md)。保留旧目录以维持历史链接。

## 启动条件

前移决定已生效，阶段位置见[执行计划](../ui-catalog-providers/plan.md)。实施前读取[总架构](../../docs/architecture/ARCHITECTURE.md)、[所有者单机架构与代码准备度](../../docs/architecture/modules/owner-host-runtime/ARCHITECTURE.md)、[数据模型](../../docs/architecture/DATA_MODEL.md)、[远程接入](../../docs/operations/REMOTE_TEAM_ACCESS.md)和[本机环境](../../docs/operations/LOCAL_DOCKER_ENVIRONMENT.md)，按用户安排进入独立实施行动。业务 PG/MinIO Adapter 已有，部署和升级实现未齐，不能直接把临时验收环境转为正式环境；备份恢复已明确不做，不再列为缺失实现。

本轮可发布目标、P1–P4 顺序与验收要求由[行动指南](../../docs/architecture/modules/owner-host-runtime/ACTION_GUIDE.md)唯一维护；下列为整体阶段门禁，不要求首版额外建设生产级运维系统。用户发布目标后只执行其明确范围，不自动领取 UI、新题或真实 API 阶段。

## 阶段任务清单

- [x] 确认课设最小运行范围、手动启动后后台运行、无备份的数据丢失风险与云存储可选；唯一规则见[运行约束](../../docs/architecture/modules/owner-host-runtime/ARCHITECTURE.md#11-已确认的课设运行约束2026-09-17)。此项只表示讨论结论，不是功能已实现。
- [x] 数据根目录已经用户确认；误删、损坏或 D 盘故障后可能全部丢失的风险已被接受，不表示实际持久化已经部署。
- [ ] 落实挂载与增长预算；不自动采购云服务、清理缓存或迁移 Docker。
- [ ] 清点真实数据与临时测试数据，冻结需要迁移的库/bucket/制品集合；只读盘点先行，避免把临时验收库当正式数据源。
- [ ] 落实长期 PostgreSQL/对象存储的固定版本与支持来源；MinIO AIStor Free 修复版方向已确认，有效许可、固定镜像和实际兼容性仍待落实。旧 CE 合成证据不作为长期部署批准。
- [ ] 规划 owner 私有数据目录/ACL、可用磁盘、数据卷与配置/秘密分离；真实 Key 不入仓库、镜像、PG 或 MinIO。
- [ ] 冻结首次 schema 基线；实际改表的后续任务提供保留旧数据的升级与前后校验，并单独说明无备份时的不可逆风险。现有 init-db 主要执行建表 SQL，不能代替迁移。
- [x] 备份/恢复、备份清单和恢复演练已由用户明确移出课设范围；这不是能力已实现，而是风险接受。
- [ ] 停启/重建/存储不可用验证：旧Job按已有证据收束、未完Run记基础设施中断、不自动续跑；重试建新Job并重新批准。整机重启另约窗口并获批准；不主动断电或强制重启用户电脑。
- [ ] 按已确认模式落实 Web/API/Worker 的手动一键启停、运行期间后台处理、可用性检查和日志留存；Worker 的单次兼容/显式循环/停止命令层已通过假测试，统一进程管理仍未实现。保持单重型 Job 和零自动重试，不安装开机自启动服务、不新增公网控制面。
- [ ] 提供正式私有 bucket、应用存储账号/权限和配置注入的显式准备步骤；避免把测试管理员凭据/临时建库夹具直接带入部署。
- [ ] 明确私有 Tailscale Serve 的终态、授权设备和开关方式，保留无 Funnel/exit node/subnet router；关闭入口不代表数据应被清理。
- [ ] 验证升级、容量告警、证据保留清理、秘密轮换和用户离职撤权；补齐运维手册、无备份风险说明与所有者验收。

## 与当前工作的分界

API代理需要的短命令牌与保守预算记录属于当前API安全边界，不是本清单中的长期部署。此清单本身不授权迁移数据、修改共享 Docker/WSL/代理/防火墙、自启动或远程网络设置；专属 D 盘写入和容器部署依用户已发布目标说明精确范围并处理审批。

首版不以接入云存储为前置，也不交付备份与恢复。持久化验收仍保留：D 盘目录完好时，正常停止/启动和容器重建后必须能读回记录与对象。误删、损坏或 D 盘故障造成的数据丢失属于用户已接受的课设风险。

## Comments

2026-09-17 目标恢复：用户明确“继续目标”。P1 已完成许可证配置红绿、两个固定摘要镜像实际拉取/版本核对和模板冻结；尚未创建正式业务目录或启动长期服务，继续以实施行动为实际证据。

2026-09-17 后续范围决定：用户明确取消备份与恢复并要求保持课设最小化。当前 P1–P4 和 `paused` 状态覆盖下列更早的 P1–P5、恢复目标与备份待办快照；恢复开发时不重新加入这些内容，除非用户另开范围。

2026-09-17 21:39 最新：用户提供 AIStor 许可并明确要求代保存；D 盘根/private/许可文件已创建，最终私有权限、结构/长度及脱敏 Free 单节点/非试用声明检查通过，服务端有效性未验证。之前的人工保存等待已解除，继续原 P1–P5，不把该文件存在记为业务持久化完成。详细失败与修正证据只在实施行动维护；下列均为历史快照。

2026-09-17 AIStor 确认后：用户明确采用 AIStor Free 修复版，原发行方向选择门禁解除，不再重复询问。继续核对官方免费许可取得方式和精确版本；方向确认不表示代用户接受条款、注册或已经取得许可证。当前具体停点与证据见实施行动。

2026-09-17 最新执行更新：用户已发布目标，并明确确认 `infra/` 部署工具箱及命令/既有接口验收。当前进入 P1 配置准备，未创建 D 盘数据或部署容器；后续不再沿用历史“等待发布”的停点。重要节点本地提交已获要求，仍不推送；剩余门禁和测试记录由实施行动维护。下列旧条目为当时快照。

2026-09-17：按用户“单独出任务清单、目前暂时没有这种想法”建立延期入口，全部未实施；未来启动时重新确认业务目标和外部操作授权。

2026-09-17：用户因五人共同使用重新打开优先级讨论。静态对照确认现有 PG/MinIO Adapter 和正式链已在隔离环境验证，但没有长期数据卷、备份恢复或进程运维；详细候选见[所有者单机运行 Module](../../docs/architecture/modules/owner-host-runtime/ARCHITECTURE.md)。当前只更新规划事实，不实施或启动任何服务。

2026-09-17：用户回复“确认”，前移决定已采纳；并要求核实全部持久化代码是否准备好。结论与源码证据在所有者单机架构第 10 节维护：业务存取已有，长期部署代码/配置未齐。仍未运行产品或发布实施任务。

2026-09-17：逐项 Grill 后用户明确容量顾虑和课设规模，并确认按课设最小方案继续，云存储暂列可选。已确认选择由运行约束维护，最新容量由本机环境第 2.1 节维护；目标目录、异机备份位置和恢复耗时仍待决定。未开通云服务或扩大实际执行授权。

2026-09-17 后续：数据根目录与一天内人工恢复目标已确认，替代上一条当时的待决状态；异机副本仍未落实。用户要求停止过细的运维 Grill，随后要求行动指南并开始，实际只做指南和源码阅读。最新要求为具体化目标交用户发布，现已整理到行动指南，不自动实施、不创建持续目标、不把 Worker 循环当作持久化交付。
